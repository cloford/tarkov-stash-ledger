"use client";
import {useEffect, useLayoutEffect, useMemo, useRef, useState} from "react";
import taskGuide from "./data/task-guide.json";
import bundledMapData from "./data/maps-cache-v3.json";
import {selectTaskReferenceImages} from "./task-media.mjs";
import KeyWiki from "./key-wiki";
import {mapLocation, normalizeMapEntries, sameMapLocation} from "./map-normalization.mjs";

type Page = "guide" | "maps" | "keys";
type MapDataResult = {maps: any[]; source: string; updatedAt?: string | null; error?: string;};
type TaskMedia = {url: string; caption: string; width?: number; height?: number;};
type MapVariant = {id: string; url: string; title: string; kind: string; width?: number; height?: number; source?: string; primary?: boolean;};
type KeyMapFocus = {keyId: string; keyName: string; mapId: string; positions: Array<{x: number; y?: number; z: number; type?: string;}>;};
declare global {interface Window {stashAI?: {media: (url: string) => Promise<TaskMedia[]>; requirements: (id: string) => Promise<{keys: any[]; verified: boolean;}>; weaponBuild: (id: string) => Promise<{builds: any[]; verified: boolean;}>; translate: (texts: string[]) => Promise<Record<string, string>>; traderPortraits: (names: string[]) => Promise<Record<string, string>>; maps: () => Promise<MapDataResult | any[]>; refreshMaps: () => Promise<MapDataResult | any[]>; cacheMapImage: (url: string, mapId: string, refresh?: boolean) => Promise<{url: string; cached: boolean; updatedAt?: string; sha256?: string; error?: string;}>; mapVariants: (map: string) => Promise<{variants: MapVariant[]; sourceUrl: string; error?: string;}>; keys: () => Promise<any>; refreshKeys: () => Promise<any>; extractImage: (map: string, extract: string) => Promise<string>; extractGuide: (map: string, extract: string) => Promise<{images: TaskMedia[]; detail: string; sourceUrl: string; searchUrl: string; verified: boolean;}>;};}}
const asset = (path: string) => typeof window !== "undefined" && window.location.protocol === "file:" ? `./${path}` : `/${path}`;
const normalizeSearchText = (value: any) => String(value ?? "").normalize("NFKC").toLocaleLowerCase("ja-JP").replace(/[^\p{L}\p{N}]/gu, "");
type MapVisual = {image: string; source: "annotated" | "raster"; size?: readonly [number, number]; revision: string;};
const mapVisuals: Record<string, MapVisual> = {
  "55f2d3fd4bdc2d5f408b4567": {image: "https://www.re3mr.com/maps/Factory/FactorybyRe3mr.png", source: "annotated", size: [13440, 6656], revision: "factory-re3mr-13440x6656-v1"},
  "56f40101d2720b2a4d8b45d6": {image: "https://maps.reemr.se/Customs/re3mrCustoms2.png", source: "annotated", size: [7832, 5016], revision: "customs-re3mr-7832x5016-v2"},
  "5704e3c2d2720bac5b8b4567": {image: "https://www.reemr.se/maps/Woods/WoodsRe3mrPNG.png", source: "annotated", size: [7680, 5168], revision: "woods-re3mr-7680x5168-v1"},
  "5704e554d2720bac5b8b456e": {image: "https://reemr.se/maps/Shoreline/re3mrShoreline2.png", source: "annotated", size: [5760, 3240], revision: "shoreline-re3mr-5760x3240-v2"},
  "5714dbc024597771384a510d": {image: "https://maps.reemr.se/Interchange/re3mrInterchange.png", source: "annotated", size: [9600, 5400], revision: "interchange-re3mr-9600x5400-v1"},
  "5b0fc42d86f7744a585f9105": {image: "https://tarkov.dev/maps/labs-2d.jpg", source: "raster", size: [3820, 1980], revision: "labs-tarkovdev-3820x1980-v1"},
  "5704e5fad2720bc05b8b4567": {image: "https://reemr.se/maps/Reserve/Re3mrReserveLossless.png", source: "annotated", size: [5760, 3240], revision: "reserve-re3mr-5760x3240-v1"},
  "5704e4dad2720bb55b8b4567": {image: "https://reemr.se/maps/Lighthouse/re3mrLighthouseVERT.png", source: "annotated", size: [8259, 7560], revision: "lighthouse-re3mr-8259x7560-v1"},
  "5714dc692459777137212e12": {image: "https://reemr.se/maps/Streets/re3mrStreetsofTarkov.png", source: "annotated", size: [7605, 4841], revision: "streets-re3mr-7605x4841-v1"},
  "653e6760052c01c1c805532f": {image: "https://www.re3mr.com/maps/Groundzero/GroundZero.png", source: "annotated", size: [2656, 2160], revision: "groundzero-re3mr-2656x2160-v1"},
  "65b8d6f5cdde2479cb2a3125": {image: "https://www.re3mr.com/maps/Groundzero/GroundZero.png", source: "annotated", size: [2656, 2160], revision: "groundzero-re3mr-2656x2160-v1"}
};
const mapBounds: Record<string, [[number, number], [number, number]]> = {
  "55f2d3fd4bdc2d5f408b4567": [[77, -64.5], [-65.5, 67.4]],
  "56f40101d2720b2a4d8b45d6": [[698, -307], [-372, 237]],
  "5704e3c2d2720bac5b8b4567": [[646, -914], [-761, 442]],
  "5704e554d2720bac5b8b456e": [[504, -415], [-1056, 618]],
  "5714dbc024597771384a510d": [[598, -442], [-433, 426]],
  "5b0fc42d86f7744a585f9105": [[-80, -477], [-287, -193]],
  "5704e5fad2720bc05b8b4567": [[289, -293], [-303, 244]],
  "5704e4dad2720bb55b8b4567": [[515, -998], [-545, 725]],
  "5714dc692459777137212e12": [[323, -295], [-280, 532]],
  "653e6760052c01c1c805532f": [[249, -124], [-99, 364]],
  "65b8d6f5cdde2479cb2a3125": [[249, -124], [-99, 364]]
};
const namedMapVisuals: Record<string, MapVisual> = {"the labyrinth": {image: "https://tarkov.dev/maps/labyrinth-2d.jpg", source: "raster", size: [4800, 4320], revision: "labyrinth-tarkovdev-4800x4320-v1"}};
const fallbackMapImages: Record<string, string> = {"55f2d3fd4bdc2d5f408b4567": "https://tarkov.dev/maps/factory-2d.jpg", "59fc81d786f774390775787e": "https://tarkov.dev/maps/factory-2d.jpg", "56f40101d2720b2a4d8b45d6": "https://tarkov.dev/maps/customs-2d.jpg", "5704e3c2d2720bac5b8b4567": "https://tarkov.dev/maps/woods-2d.jpg", "5704e554d2720bac5b8b456e": "https://www.escapistmagazine.com/wp-content/uploads/2023/07/Escape-from-Tarkov-Shoreline-Map.jpg?fit=1200%2C800", "5714dbc024597771384a510d": "https://tarkov.dev/maps/interchange-2d.jpg", "5704e4dad2720bb55b8b4567": "https://tarkov.dev/maps/lighthouse-2d.jpg", "5704e5fad2720bc05b8b4567": "https://tarkov.dev/maps/reserve-2d.jpg", "5714dc692459777137212e12": "https://tarkov.dev/maps/streets-2d.jpg", "653e6760052c01c1c805532f": "https://tarkov.dev/maps/ground-zero-2d.jpg", "65b8d6f5cdde2479cb2a3125": "https://tarkov.dev/maps/ground-zero-2d.jpg", "68236e8153654e8c1200798a": "https://tarkov.dev/maps/ground-zero-2d.jpg", "5b0fc42d86f7744a585f9105": "https://tarkov.dev/maps/labs-2d.jpg", "6a294a5b5eb5f9a1700417b7": "https://tarkov.dev/maps/labs-2d.jpg"};
const fallbackMapImagesByName: Record<string, string> = {factory: "https://tarkov.dev/maps/factory-2d.jpg", "night factory": "https://tarkov.dev/maps/factory-2d.jpg", customs: "https://tarkov.dev/maps/customs-2d.jpg", woods: "https://tarkov.dev/maps/woods-2d.jpg", shoreline: "https://tarkov.dev/maps/shoreline-2d.jpg", interchange: "https://tarkov.dev/maps/interchange-2d.jpg", lighthouse: "https://tarkov.dev/maps/lighthouse-2d.jpg", reserve: "https://tarkov.dev/maps/reserve-2d.jpg", "streets of tarkov": "https://tarkov.dev/maps/streets-2d.jpg", streets: "https://tarkov.dev/maps/streets-2d.jpg", "ground zero": "https://tarkov.dev/maps/ground-zero-2d.jpg", "ground zero 21+": "https://tarkov.dev/maps/ground-zero-2d.jpg", "ground zero tutorial": "https://tarkov.dev/maps/ground-zero-2d.jpg", "the lab": "https://tarkov.dev/maps/labs-2d.jpg", "the lab (dark)": "https://tarkov.dev/maps/labs-2d.jpg", labs: "https://tarkov.dev/maps/labs-2d.jpg", icebreaker: "https://tarkov.dev/maps/icebreaker-2d.jpg", terminal: "https://tarkov.dev/maps/terminal-2d.jpg", "the labyrinth": "https://tarkov.dev/maps/labyrinth-2d.jpg"};
const mapVisualFor = (map: any) => mapVisuals[String(map.id || "")] || namedMapVisuals[String(map.name || map.nameJa || "").trim().toLowerCase()];
const mapImageFor = (map: any) => {const id = String(map.id || ""), name = String(map.name || map.nameJa || "").trim().toLowerCase(), slug = String(map.slug || name).trim().toLowerCase().replace(/\+/g, "").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); return mapVisualFor(map)?.image || fallbackMapImages[id] || fallbackMapImagesByName[name] || fallbackMapImagesByName[String(map.slug || "").toLowerCase()] || `https://tarkov.dev/maps/${slug}-2d.jpg`;};
const taskDataRevision = "eft-1.1-live-2026-08-19";
const liveTaskCorrections: Record<string, {verifiedAt: string; note: string; removeObjectives?: string[];}> = {
  "66aa74571e5e199ecd094f18": {verifiedAt: "2026-08-18", note: "ゲーム内タスク画面（Patch 1.1）で確認", removeObjectives: ["66ab97d56cb6e3bfd7c79fbc"]}
};
function applyLiveTaskCorrections(task: any) {const correction = liveTaskCorrections[String(task?.id || "")]; if (!correction) return task; const remove = new Set(correction.removeObjectives || []); return {...task, objectives: (task.objectives || []).filter((objective: any) => !remove.has(String(objective.id))), liveVerification: {verifiedAt: correction.verifiedAt, note: correction.note, revision: taskDataRevision}};}
function normalizeTaskGuide(value: any) {if (!value || !Array.isArray(value.tasks)) return value; return {...value, dataRevision: taskDataRevision, tasks: value.tasks.map((rawTask: any) => {const task = applyLiveTaskCorrections(rawTask); return {...task, objectives: (task.objectives || []).map((objective: any) => ({...objective, maps: objectiveMapsFor(task, objective)}))};})};}

export default function Home() {
  const [page, setPage] = useState<Page>("guide");
  const [guideReset, setGuideReset] = useState(0);
  useEffect(() => {const valid = (value: any): value is Page => ["guide", "maps", "keys"].includes(value); if (!valid(history.state?.stashPage)) history.replaceState({...history.state, stashPage: "guide"}, ""); const restore = (event: PopStateEvent) => {if (valid(event.state?.stashPage)) setPage(event.state.stashPage);}; window.addEventListener("popstate", restore); return () => window.removeEventListener("popstate", restore);}, []);
  const openPage = (next: Page) => {if (next === "guide") {const state = {...history.state, stashPage: "guide", guideSection: "tasks", guideView: "traders", guideSelected: "", guideTrader: ""}; if (page === "guide") history.replaceState(state, ""); else history.pushState(state, ""); setPage("guide"); setGuideReset(value => value + 1); return ;} if (next !== page) {let state: any = {...history.state, stashPage: next}; try {const last = JSON.parse(sessionStorage.getItem("tarkov-map-last-view") || "{}"); state = {...state, mapStage: last.selected || ""};} catch {state = {...state, mapStage: ""};} history.pushState(state, "");} setPage(next);};
  const openMapFromKey = (mapName: string, keyFocus?: KeyMapFocus) => {const stage = mapStageFor(mapName), normalizedFocus = keyFocus && stage?.id ? {...keyFocus, mapId: stage.id} : keyFocus || null; history.pushState({...history.state, stashPage: "maps", mapStage: stage?.name || mapName, mapKeyFocus: normalizedFocus}, ""); setPage("maps");};
  const openTaskFromKey = (taskId: string, trader: string) => {history.pushState({...history.state, stashPage: "guide", guideSection: "tasks", guideView: "detail", guideSelected: taskId, guideTrader: trader}, ""); setPage("guide"); setGuideReset(value => value + 1);};
  return <main><header className="hero compact"><div className="eyebrow">ESCAPE FROM TARKOV · TASK & EXTRACTION GUIDE</div><div className="heroRow"><div><h1>TARKOV <span>NAVI</span></h1><p>タスクと脱出地点を、迷わず確認。</p></div></div></header>
    <nav className="mainNav">{([["guide", "タスク情報"], ["maps", "MAP"], ["keys", "鍵WIKI"]] as const).map(([key, label], index) => <button className={page === key ? "active" : ""} onClick={() => openPage(key)} key={key}><span>0{index + 1}</span>{label}</button>)}</nav>
    {page === "guide" && <TaskGuidePage key={guideReset} />}
    {page === "maps" && <EnhancedMapHub />}
    {page === "keys" && <KeyWiki onOpenTask={openTaskFromKey} />}
    <footer>DATA: TARKOVDATA · TARKOV.DEV · EFT WIKI <span>タスク・マップ・鍵の情報を表示します</span></footer></main>;
}

const mapStages = [
  {name: "Ground Zero", ja: "グラウンドゼロ", id: "653e6760052c01c1c805532f", desc: "市街地中心部。建物内と地下通路が多く、初心者は現在地の確認が重要です。", exits: ["Emercom Checkpoint", "Mira Ave", "Nakatani Basement Stairs"]},
  {name: "Streets of Tarkov", ja: "ストリート・オブ・タルコフ", id: "5714dc692459777137212e12", desc: "広大な市街地。通り名と大きな建物を目印に移動します。", exits: ["Damaged House", "Evacuation Zone", "Klimov Street"]},
  {name: "Customs", ja: "カスタム", id: "56f40101d2720b2a4d8b45d6", desc: "道路と川で東西に分かれた定番マップ。赤倉庫・寮・新建築を覚えると迷いにくくなります。", exits: ["Crossroads", "ZB-1011", "RUAF Roadblock"]},
  {name: "Woods", ja: "ウッズ", id: "5704e3c2d2720bac5b8b4567", desc: "森林中心のマップ。山・湖・製材所・送電線で方向を確認します。", exits: ["Outskirts", "UN Roadblock", "ZB-014"]},
  {name: "Interchange", ja: "インターチェンジ", id: "5714dbc024597771384a510d", desc: "巨大商業施設ULTRAが中心。店舗名と屋外の方角をセットで覚えます。", exits: ["Emercom Checkpoint", "Railway Exfil", "Hole in the Fence"]},
  {name: "Factory", ja: "ファクトリー", id: "55f2d3fd4bdc2d5f408b4567", desc: "小規模な屋内戦マップ。扉番号・地下・オフィス階段が主な目印です。", exits: ["Gate 3", "Cellars", "Med Tent Gates"]},
  {name: "Reserve", ja: "リザーブ", id: "5704e5fad2720bc05b8b4567", desc: "軍事基地。建物のチェス記号と地下通路を覚えると移動しやすくなります。", exits: ["D-2", "Cliff Descent", "Armored Train"]},
  {name: "Lighthouse", ja: "ライトハウス", id: "5704e4dad2720bb55b8b4567", desc: "海岸沿いに細長いマップ。幹線道路・山側・浄水場の位置関係が重要です。", exits: ["Path to Shoreline", "Northern Checkpoint", "Southern Road"]},
  {name: "Shoreline", ja: "ショアライン", id: "5704e554d2720bac5b8b456e", desc: "保養所を中心とした広い自然マップ。海岸・川・発電所で方角を確認します。", exits: ["Path to Lighthouse", "Road to Customs", "Tunnel"]},
  {name: "The Lab", ja: "研究所", id: "5b0fc42d86f7744a585f9105", desc: "高難度の屋内施設。各区画の色とエレベーター、電源操作を確認します。", exits: ["Cargo Elevator", "Medical Block Elevator", "Parking Gate"]},
  {name: "Terminal", ja: "ターミナル", id: "", desc: "港湾ターミナル。遮蔽物と建物番号を確認して移動します。", exits: []},
  {name: "Icebreaker", ja: "アイスブレーカー", id: "", desc: "船内を進む特殊マップ。甲板・機関室・通路階層を確認します。", exits: []},
  {name: "The Labyrinth", ja: "ラビリンス", id: "", desc: "複雑な閉所マップ。入口、分岐、階層を一つずつ確認します。", exits: []}
];
function mapStageFor(reference: any) {return mapStages.find(stage => sameMapLocation(stage, typeof reference === "string" ? {name: reference} : reference));}
const stageHotspots = [
  {name: "Streets of Tarkov", left: 40.2, top: 15.6, width: 18, height: 6}, {name: "The Lab", left: 28.8, top: 18.7, width: 9, height: 6},
  {name: "Interchange", left: 41.8, top: 32.1, width: 14, height: 6}, {name: "Woods", left: 25.9, top: 37, width: 11, height: 6},
  {name: "Customs", left: 38.6, top: 39.6, width: 12, height: 6}, {name: "Factory", left: 30.2, top: 49.2, width: 10, height: 6},
  {name: "Reserve", left: 19.8, top: 57.8, width: 10, height: 6}, {name: "Lighthouse", left: 18.6, top: 70.8, width: 13, height: 6},
  {name: "Shoreline", left: 25.2, top: 81, width: 12, height: 6}, {name: "Terminal", left: 35.4, top: 78.2, width: 12, height: 6}
];
const factoryExtracts = [
  {name: "Courtyard Gate", faction: "PMC", direction: "西（W）", requirement: "鍵不要・常時利用可能", description: "屋外のトラック前にある中庭ゲート。トランジット判定より奥まで進むと脱出判定に入ります。", image: "https://tarkov101.com/wp-content/uploads/2024/10/FactoryToWoods.png"},
  {name: "Cellars", faction: "PMC", direction: "北東（NE）・地下", requirement: "Factory emergency exit keyが必要", description: "区画2側から地下道へ降り、階段裏の扉を抜けた先。誰かが開錠済みなら鍵なしでも通れます。", image: "https://primagames.com/wp-content/uploads/2024/08/Factory-Gate-2-Going-to-cellars-EFT.jpg?resize=1024"},
  {name: "Gate 0", faction: "PMC", direction: "南西（SW）", requirement: "鍵不要・常時利用可能", description: "地上階の大きな「0」表示が目印。Ver.0.15以降は鍵なしで利用できます。", image: "https://www.escapistmagazine.com/wp-content/uploads/2023/06/Escape-From-Tarkov-Scout-Gate-0-Extract.png"},
  {name: "Med Tent Gate", faction: "PMC", direction: "南東（SE）", requirement: "Factory emergency exit keyが必要", description: "医療テントの奥にある赤いゲート。テントと壁の隙間からゲート内へ入れます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/FACTORY-ESC-MED_TENT_GATES-others_3.jpg.webp?rev=68581170cc1a7b561fed7119135926f7&t=20231203163300"},
  {name: "Camera Bunker Door", faction: "Scav", direction: "南西（SW）・地下", requirement: "SCAV専用・条件なし", description: "地下バンカーの監視カメラ付近にある扉。暗い地下道と積まれた樽が目印です。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aattach/FACTORY-ESC-CAMERA_BUNKER_DOOR-Rt_Door.jpg?rev=effa8d499f05df11981ff1748a97e0ce&t=20231013142425"},
  {name: "Office Window", faction: "Scav", direction: "西（W）・3階", requirement: "SCAV専用・条件なし", description: "オフィス3階の窓際。机、PC、壊れたブラインドがある部屋の窓側に立ちます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aattach/FACTORY-ESC-OFFICE_WINDOW-Rt_Esc.jpg?rev=134005d2d934e07ea71875e1a5fe3dba&t=20231013143355"},
  {name: "Gate 3", faction: "All", direction: "北西（NW）", requirement: "PMC・SCAV共通・条件なし", description: "大きな数字の3がある赤いゲート。最初のゲートから青い箱の区画を通り、さらに奥の扉へ進みます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/Gate-3_1_ver0.15.png.webp?rev=4f7cf99cc5f5b1e81654a23eefea7cee&t=20241226192904"},
  {name: "Smugglers' Passage", faction: "All", direction: "東（E）・地下", requirement: "Note with code word Arkが必要", description: "Cellarsへ向かう扉と逆方向の地下通路。レイド内でArkのメモを見つけ、出口前で引き渡します。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/FactoryMap.jpg?rev=67da37a9b06e89e3b94bfbe58b2c57b6&t=20180818200634"}
];
const labExtracts = [
  {name: "Medical Block Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Cargo Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Main Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Ventilation Shaft", faction: "PMC", requirement: "バックパック装備不可"},
  {name: "Sewage Conduit", faction: "PMC", requirement: "排水操作が必要"},
  {name: "Parking Gate", faction: "PMC", requirement: "ゲート開放操作が必要"},
  {name: "Hangar Gate", faction: "PMC", requirement: "ゲート開放操作が必要"}
];
const labyrinthExtracts = [
  {name: "The Way Up", faction: "PMC"},
  {name: "Ariadne's Path", faction: "PMC"}
];
type PercentAnnotationPart = {left: number; top: number; width: number; height: number;};
type PercentAnnotationAnchor = {left: number; top: number; width?: number; height?: number; focusLeft?: number; focusTop?: number; parts?: PercentAnnotationPart[];};
type PixelRect = {x: number; y: number; width: number; height: number;};
type MapAnnotation = {focusPoint: {x: number; y: number;}; labelBounds: PixelRect[]; hitBounds: PixelRect[]; lines: number; revision: string;};
const printedLabelKey = (value: any) => String(value || "").normalize("NFKC").toLowerCase().replace(/[・･]/g, "").replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]/g, "");
const printedLabelAnchors: Record<string, Record<string, PercentAnnotationAnchor>> = {
  "5714dc692459777137212e12": {
    expocheckpoint: {left: 54.4, top: 17.64}, cardinalapartmentcomplexparking: {left: 61.02, top: 16.74}, stylobatebuildingelevator: {left: 76.8, top: 26.64}, klimovshoppingmallexfil: {left: 87.29, top: 35.86}, klimovstreetflare: {left: 91.96, top: 49.28}, pinewoodbasementcoop: {left: 81.89, top: 43.19}, pinewoodbasement: {left: 81.89, top: 43.19}, sewerriver: {left: 91.29, top: 61.65}, entrancetocatacombs: {left: 91.68, top: 70.3}, damagedhouse: {left: 87.65, top: 76.99}, ventilationshaft: {left: 77.44, top: 83.83}, courtyard: {left: 77.13, top: 98.32}, primorskyavetaxivex: {left: 62.78, top: 93.98}, crashsite: {left: 37.51, top: 81.14}, sewermanhole: {left: 42.17, top: 67.18}, collapsedcrane: {left: 46.79, top: 60.3}, nearkamchatskayaarch: {left: 47.11, top: 33.7, width: 6.92, height: 3.45}, smugglersbasement: {left: 64.3, top: 40.89}, グラウンドゼロへ移動: {left: 93.43, top: 53.21}, インターチェンジへ移動: {left: 35.39, top: 87.24}, 研究所へ移動: {left: 50.27, top: 41.47}
  },
  "56f40101d2720b2a4d8b45d6": {
    zb013: {left: 51.25, top: 28.4, width: 2.2, height: 0.9}, dormsvex: {left: 51.59, top: 75.73}, zb1011: {left: 6.91, top: 30.68}, crossroads: {left: 97.03, top: 47.03}, oldgasstation: {left: 35.5, top: 27.14}, trailerpark: {left: 94.95, top: 24.01}, ruafroadblock: {left: 66.8, top: 32.75, width: 4.1, height: 2.7, focusLeft: 66.8, focusTop: 33.2}, militarybasecp: {left: 6.39, top: 69.06}, militarycheckpoint: {left: 6.09, top: 66.53}, passagebetweenrocks: {left: 13.32, top: 73.39}, railroadtomilitarybase: {left: 20.24, top: 75.2}, oldroadgate: {left: 44.83, top: 75.33}, sniperroadblock: {left: 61.34, top: 66.51}, railroadtoport: {left: 78.62, top: 60}, trailerparkworkersshack: {left: 89.15, top: 23.67}, railroadtotarkov: {left: 79.98, top: 24.95}, warehouse17: {left: 63.1, top: 35.81}, factoryshacks: {left: 53.18, top: 32.4}, warehouse4: {left: 31.65, top: 34.35, width: 3.15, height: 2.13}, oldgasstationgate: {left: 35.5, top: 27.14, width: 4.5, height: 2.2}, factoryfarcorner: {left: 4.11, top: 32.65}, administrationgate: {left: 2.16, top: 43.68, width: 3.99, height: 1.35}, railroadpassageflare: {left: 48.47, top: 22}, scavcheckpoint: {left: 3.68, top: 51.88, width: 2.97, height: 2.34}, boilerroombasementcoop: {left: 56.2, top: 30.2, width: 5.5, height: 2.8}, smugglersboat: {left: 72.11, top: 67.72}, smugglersbunkerzb1012: {left: 22.43, top: 32.12}, リザーブへ移動: {left: 3.99, top: 67.96}, ファクトリーへ移動: {left: 33.4, top: 27.23}, インターチェンジへ移動: {left: 97.66, top: 25.77}, ショアラインへ移動: {left: 65.12, top: 66.65}
  },
  "5704e3c2d2720bac5b8b4567": {
    friendshipbridgecoop: {left: 44.05, top: 8.53}, ruafgate: {left: 54.24, top: 74.89}, ruafroadblock: {left: 54.24, top: 74.89}, zb016: {left: 73.28, top: 52.96}, zb014: {left: 19.4, top: 56.9, width: 2.15, height: 1}, unroadblock: {left: 85.3, top: 67.7, width: 4.35, height: 1.15}, northernunroadblock: {left: 85.4, top: 50.6, width: 4.5, height: 1.9}, outskirts: {left: 24.1, top: 74.8}, bridgevex: {left: 81.97, top: 27.14}, deadmansplace: {left: 38.15, top: 67.1, width: 5.8, height: 1.35}, boat: {left: 37.33, top: 65.52}, scavhouse: {left: 17.21, top: 65.87}, scavbunker: {left: 51.05, top: 48.05, width: 12, height: 2.6}, easternrocks: {left: 83.8, top: 53.45, width: 2.8, height: 1.9}, mountainstash: {left: 64.2, top: 41.68}, oldrailwaydepot: {left: 79.79, top: 61}, powerlinepassageflare: {left: 14.49, top: 52.41}, railwaybridgetotarkov: {left: 96.73, top: 66.28}, ファクトリーへ移動: {left: 72.5, top: 72.49}, リザーブへ移動: {left: 31.43, top: 73.84}, ライトハウスへ移動: {left: 16.13, top: 72.3}, カスタムへ移動: {left: 56.39, top: 73.22}
  },
  "5704e554d2720bac5b8b456e": {
    pathtolighthouse: {left: 19.06, top: 16.38}, roadtocustoms: {left: 83.23, top: 32.18}, tunnel: {left: 16.12, top: 47.65}, roadtonorthvex: {left: 64.26, top: 3.79}, railwaybridge: {left: 92.73, top: 47.48}, climberstrail: {left: 48.45, top: 5.23}, pierboat: {left: 56.75, top: 73.6, width: 3.7, height: 1.55}, smugglerspathcoop: {left: 73.07, top: 9.95}, adminbasement: {left: 51.07, top: 14.54}, eastwinggymentrance: {left: 56.85, top: 19.62}, lighthouse: {left: 63.46, top: 65.91}, ruinedroad: {left: 17.32, top: 55.85}, mountainbunker: {left: 55.5, top: 7.66}, ライトハウスへ移動: {left: 19.06, top: 16.38}, ターミナルへ移動: {left: 88.14, top: 56.84}, ラビリンスへ移動: {left: 49.18, top: 22.25}
  },
  "55f2d3fd4bdc2d5f408b4567": {
    courtyardgate: {left: 46.87, top: 30.6}, cellars: {left: 92.64, top: 14.48}, gate0: {left: 53.14, top: 65.26}, medtentgate: {left: 71.97, top: 51.3}, camerabunkerdoor: {left: 82.66, top: 47.09}, officewindow: {left: 2.64, top: 34.4}, gate3: {left: 49.84, top: 22.56}, smugglerspassage: {left: 41.23, top: 5.8}
  },
  "653e6760052c01c1c805532f": {
    emercomcheckpoint: {left: 59.86, top: 9.22}, scavcheckpointcoop: {left: 75.84, top: 10.9}, miraaveflare: {left: 50.62, top: 32.13}, policecordonvex: {left: 82.25, top: 42.86, width: 8.5, height: 3.88}, nakatanibasementstairs: {left: 81.3, top: 78.4, width: 13, height: 3, parts: [{left: 75.68, top: 85.32, width: 8.2, height: 1.8}]}, tartowerssalesoffice: {left: 54.7, top: 59.51}, ストリートオブタルコフへ移動: {left: 48.19, top: 42.07}
  },
  "65b8d6f5cdde2479cb2a3125": {
    emercomcheckpoint: {left: 59.86, top: 9.22}, scavcheckpointcoop: {left: 75.84, top: 10.9}, miraaveflare: {left: 50.62, top: 32.13}, policecordonvex: {left: 82.25, top: 42.86, width: 8.5, height: 3.88}, nakatanibasementstairs: {left: 81.3, top: 78.4, width: 13, height: 3, parts: [{left: 75.68, top: 85.32, width: 8.2, height: 1.8}]}, tartowerssalesoffice: {left: 54.7, top: 59.51}, ストリートオブタルコフへ移動: {left: 48.19, top: 42.07}
  },
  "5714dbc024597771384a510d": {
    emercomcheckpoint: {left: 49.48, top: 80.04}, railwayexfil: {left: 5.83, top: 12.51}, powerstationvex: {left: 50.31, top: 25.5}, scavcampcoop: {left: 67.18, top: 47.23, width: 4.38, height: 2.47}, saferoomexfil: {left: 66.58, top: 54.68}, カスタムへ移動: {left: 17.97, top: 96.35}, ストリートオブタルコフへ移動: {left: 17.21, top: 14.9}
  },
  "5704e5fad2720bc05b8b4567": {
    armoredtrain: {left: 26.55, top: 25.48}, d2: {left: 97.52, top: 88.41}, bunkerhermeticdoor: {left: 37.24, top: 18.64, width: 6.31, height: 3.51}, cliffdescent: {left: 58.36, top: 72.56, width: 3.7, height: 1.3}, scavlandscoop: {left: 68.93, top: 6.09, width: 4.47, height: 2.54}, sewermanhole: {left: 43.2, top: 48.88, width: 7.72, height: 2.65}, holeinthewallbythemountains: {left: 91.65, top: 31.28}, heatingpipe: {left: 51.09, top: 2.18}, depothermeticdoor: {left: 26.65, top: 34.74}, checkpointfence: {left: 50.25, top: 69.61, width: 4.99, height: 3.37}, exittowoods: {left: 36.52, top: 5.33}, ライトハウスへ移動: {left: 12.37, top: 26.5}, ウッズへ移動: {left: 12.24, top: 14.75, width: 2.57, height: 1.35}, カスタムへ移動: {left: 74.28, top: 5.24, width: 3.06, height: 2.09}
  },
  "5704e4dad2720bb55b8b4567": {
    roadtomilitarybasevex: {left: 92.36, top: 14.14}, sidetunnelcoop: {left: 72.21, top: 77.05}, mountainpass: {left: 78.34, top: 54.67}, pathtoshoreline: {left: 91.14, top: 50.34}, northerncheckpoint: {left: 57.57, top: 1.72}, southernroad: {left: 84.6, top: 79.6}, southernroadlandslide: {left: 83.98, top: 84.29}, hideoutunderthelandingstage: {left: 51.85, top: 71.3}, scavhideoutatthegrotto: {left: 52.37, top: 29.02}, industrialzonegates: {left: 77.06, top: 11.74}, armoredtrain: {left: 67.07, top: 9.39}, passagebythelake: {left: 91.03, top: 25.06}, ショアラインへ移動: {left: 90.51, top: 50.98}, リザーブへ移動: {left: 83.79, top: 12.91}, ウッズへ移動: {left: 53.72, top: 6.61}
  },
  "5b0fc42d86f7744a585f9105": {
    mainelevator: {left: 17.25, top: 1.95, width: 6.8, height: 1.7}, ventilationshaft: {left: 4.11, top: 40.38, width: 7, height: 2.37}, medicalblockelevator: {left: 16.44, top: 62.17, width: 9, height: 2.2}, sewageconduit: {left: 22.79, top: 51.82, width: 6.8, height: 2.2}, parkinggate: {left: 30.35, top: 65.3, width: 5.4, height: 2.4}, cargoelevator: {left: 72.87, top: 58.33, width: 6.2, height: 2.4}, hangargate: {left: 71.64, top: 77.32, width: 5.2, height: 2.4}
  },
  "name:thelabyrinth": {
    thewayup: {left: 79, top: 43.7, width: 5.2, height: 1.35}, ariadnespath: {left: 44.14, top: 64.27, width: 8.2, height: 1.35}
  }
};
const unprintedLabelKeys: Record<string, Set<string>> = {
  "5714dbc024597771384a510d": new Set(["holeinthefence"]),
  "5b0fc42d86f7744a585f9105": new Set(["ストリートオブタルコフへ移動"])
};
function printedLabelLineLengths(value: any) {
  const label = String(value || "").trim(), compact = label.replace(/\s+/g, "");
  let lineCount = Math.min(3, Math.max(1, Math.ceil(compact.length / 22)));
  if (lineCount === 1) return [label.length];
  const words = label.split(/\s+/).filter(Boolean);
  if (words.length === 1) {const chunk = Math.ceil(label.length / lineCount); return Array.from({length: lineCount}, (_, index) => label.slice(index * chunk, (index + 1) * chunk).length);}
  lineCount = Math.min(lineCount, words.length); let best: number[] | null = null, bestScore = Infinity;
  const search = (start: number, lengths: number[]) => {if (lengths.length === lineCount - 1) {const tail = words.slice(start).join(" ").length, candidate = [...lengths, tail], max = Math.max(...candidate), min = Math.min(...candidate), score = max * 10 + (max - min); if (score < bestScore) {best = candidate; bestScore = score;} return ;} const remainingLines = lineCount - lengths.length - 1; for (let end = start + 1; end <= words.length - remainingLines; end++)search(end, [...lengths, words.slice(start, end).join(" ").length]);};
  search(0, []); return best || [label.length];
}
function printedLabelAnchor(mapId: string, mapName: string, name: any): MapAnnotation | null {
  const points = printedLabelAnchors[mapId] || printedLabelAnchors[`name:${printedLabelKey(mapName)}`], point = points?.[printedLabelKey(name)];
  const visual = mapVisuals[mapId] || namedMapVisuals[String(mapName || "").trim().toLowerCase()];
  if (!point || !visual?.size) return null;
  const lines = printedLabelLineLengths(name), longest = Math.max(...lines, 1), lineCount = lines.length;
  const baseWidth = point.width ?? Math.min(10, Math.max(2.6, longest * .44 + .8));
  const baseHeight = Math.max(point.height ?? 0, 1.2 + (lineCount - 1) * 1.75), [imageWidth, imageHeight] = visual.size;
  const toPixels = (part: PercentAnnotationPart | PercentAnnotationAnchor, width: number, height: number): PixelRect => ({x: (part.left - width / 2) / 100 * imageWidth, y: (part.top - height / 2) / 100 * imageHeight, width: width / 100 * imageWidth, height: height / 100 * imageHeight});
  const labelBounds = [toPixels(point, baseWidth, baseHeight), ...(point.parts || []).map(part => toPixels(part, part.width, part.height))];
  const hitBounds = labelBounds.map(rect => {const padX = Math.max(40, rect.width * .18), padY = Math.max(32, rect.height * .45); return {x: Math.max(0, rect.x - padX), y: Math.max(0, rect.y - padY), width: Math.min(imageWidth, rect.width + padX * 2), height: Math.min(imageHeight, rect.height + padY * 2)};});
  return {focusPoint: {x: (point.focusLeft ?? point.left) / 100 * imageWidth, y: (point.focusTop ?? point.top) / 100 * imageHeight}, labelBounds, hitBounds, lines: lineCount, revision: visual.revision};
}
function pixelRectStyle(rect: PixelRect, size: readonly [number, number]) {return {left: `${rect.x / size[0] * 100}%`, top: `${rect.y / size[1] * 100}%`, width: `${rect.width / size[0] * 100}%`, height: `${rect.height / size[1] * 100}%`};}
function OfflineZoomMap({selected, selectedExtract, keyFocus, onShowDetails}: {selected: any; selectedExtract: any; keyFocus?: KeyMapFocus | null; onShowDetails: () => void;}) {
  const viewportRef = useRef<HTMLDivElement | null>(null), markerRef = useRef<HTMLSpanElement | null>(null), dragRef = useRef<{x: number; y: number; startX: number; startY: number;} | null>(null), didDragRef = useRef(false), panCleanupRef = useRef<() => void>(() => {}), zoomAnchorRef = useRef<{x: number; y: number; localX: number; localY: number;} | null>(null), focusRequestRef = useRef("");
  const primaryUrl = mapImageFor({id: selected.id, name: selected.name, slug: selected.name.toLowerCase().replace(/\s+/g, "-")}), primaryVisual = mapVisualFor(selected), primaryVariant: MapVariant = {id: "extraction", url: primaryUrl, title: "脱出地点・タスク対応マップ", kind: "基準版", width: primaryVisual?.size?.[0], height: primaryVisual?.size?.[1], source: "RE3MR / tarkov.dev", primary: true};
  const [variants, setVariants] = useState<MapVariant[]>([primaryVariant]), [activeVariantId, setActiveVariantId] = useState("extraction"), [variantsLoading, setVariantsLoading] = useState(false), [variantsSourceUrl, setVariantsSourceUrl] = useState("");
  const activeVariant = variants.find(variant => variant.id === activeVariantId) || variants[0] || primaryVariant, remoteUrl = activeVariant.url, visual = activeVariant.primary ? primaryVisual : undefined, activeVariantIndex = Math.max(0, variants.findIndex(variant => variant.id === activeVariant.id)), sameKindOrdinal = activeVariant.primary ? 0 : variants.slice(0, activeVariantIndex + 1).filter(variant => variant.kind === activeVariant.kind).length, stableVariantTitle = activeVariant.primary ? "脱出地点・タスク対応マップ" : `${selected.ja} · ${activeVariant.kind}${sameKindOrdinal > 1 ? ` ${sameKindOrdinal}` : ""}`;
  const [zoom, setZoom] = useState(1), [panning, setPanning] = useState(false), [imageUrl, setImageUrl] = useState(remoteUrl), [imageSize, setImageSize] = useState<readonly [number, number] | null>(null), [cached, setCached] = useState(false), [saving, setSaving] = useState(false), [message, setMessage] = useState(""), [imageHash, setImageHash] = useState(""), [imageError, setImageError] = useState(false);
  const clampZoom = (value: number) => Math.max(1, Math.min(3, value));
  const sizeMatches = !visual?.size || Boolean(imageSize && imageSize[0] === visual.size[0] && imageSize[1] === visual.size[1]), selectedPoint = activeVariant.primary && sizeMatches ? printedLabelAnchor(selected.id, selected.name, selectedExtract?.name) : null, bounds = mapBounds[selected.id], keyPositions = activeVariant.primary && keyFocus?.mapId === selected.id && bounds ? (keyFocus.positions || []).map((position, index) => {const [[startX, startZ], [endX, endZ]] = bounds; return {index, left: Math.max(1, Math.min(99, (position.x - startX) / (endX - startX) * 100)), top: Math.max(1, Math.min(99, (position.z - startZ) / (endZ - startZ) * 100)), raw: position};}) : [], focusTarget = selectedPoint ? {left: selectedPoint.focusPoint.x / (visual?.size?.[0] || 1) * 100, top: selectedPoint.focusPoint.y / (visual?.size?.[1] || 1) * 100} : keyPositions[0] || null, selectionKey = selectedExtract ? `${selected.id || selected.name}-${activeVariant.id}-${selectedExtract.id || selectedExtract.name}` : keyFocus && keyPositions.length ? `${selected.id}-${activeVariant.id}-${keyFocus.keyId}` : "", selectedLabelNotPrinted = Boolean(activeVariant.primary && unprintedLabelKeys[selected.id]?.has(printedLabelKey(selectedExtract?.name))), auditEnabled = typeof window !== "undefined" && new URLSearchParams(window.location.search).has("mapAudit"), annotationSource = printedLabelAnchors[selected.id] || printedLabelAnchors[`name:${printedLabelKey(selected.name)}`] || {}, auditAnnotations = auditEnabled && activeVariant.primary && sizeMatches ? Object.keys(annotationSource).map(key => printedLabelAnchor(selected.id, selected.name, key)).filter(Boolean) as MapAnnotation[] : [];
  useEffect(() => {let cancelled = false; setVariants([primaryVariant]); setActiveVariantId("extraction"); setVariantsSourceUrl(""); if (!window.stashAI?.mapVariants) return; setVariantsLoading(true); window.stashAI.mapVariants(selected.name).then(result => {if (cancelled) return; const primaryFileKey = printedLabelKey(decodeURIComponent(primaryUrl.split("/").pop()?.split("?")[0] || "")); const alternatives = (result.variants || []).filter(variant => variant.url !== primaryUrl && printedLabelKey(variant.title) !== primaryFileKey); setVariants([primaryVariant, ...alternatives]); setVariantsSourceUrl(result.sourceUrl || "");}).catch(() => {}).finally(() => {if (!cancelled) setVariantsLoading(false);}); return () => {cancelled = true;};}, [selected.id, selected.name, primaryUrl]);
  useEffect(() => {let cancelled = false; setZoom(1); setImageUrl(remoteUrl); setImageSize(null); setCached(false); setMessage(""); setImageHash(""); setImageError(false); if (viewportRef.current) {viewportRef.current.scrollLeft = 0; viewportRef.current.scrollTop = 0;} window.stashAI?.cacheMapImage?.(remoteUrl, `${selected.id || selected.name}-${activeVariant.id}`, false).then(result => {if (cancelled) return; if (result.cached) {setImageUrl(result.url); setCached(true); setImageHash(result.sha256 || ""); setMessage("保存済み画像を表示中");} }).catch(() => {}); return () => {cancelled = true;};}, [selected.id, selected.name, activeVariant.id, remoteUrl]);
  const saveOffline = async () => {if (!window.stashAI?.cacheMapImage) {setMessage("配布版アプリで利用できます"); return ;} setSaving(true); setMessage("保存中…"); try {const result = await window.stashAI.cacheMapImage(remoteUrl, `${selected.id || selected.name}-${activeVariant.id}`, true); if (!result.cached) throw Error(result.error || "保存失敗"); setImageUrl(result.url); setCached(true); setImageHash(result.sha256 || ""); setMessage("オフライン保存完了");} catch {setMessage("保存できませんでした。現在の画像を維持します");} finally {setSaving(false);} };
  const resetView = () => {zoomAnchorRef.current = null; focusRequestRef.current = ""; setZoom(1); if (viewportRef.current) {viewportRef.current.scrollLeft = 0; viewportRef.current.scrollTop = 0;} };
  const zoomAt = (nextZoom: number, clientX?: number, clientY?: number) => {const viewport = viewportRef.current, next = clampZoom(nextZoom); if (!viewport || next === zoom) {setZoom(next); return ;} const rect = viewport.getBoundingClientRect(), localX = clientX === undefined ? viewport.clientWidth / 2 : clientX - rect.left, localY = clientY === undefined ? viewport.clientHeight / 2 : clientY - rect.top; zoomAnchorRef.current = {x: (viewport.scrollLeft + localX) / Math.max(1, viewport.scrollWidth), y: (viewport.scrollTop + localY) / Math.max(1, viewport.scrollHeight), localX, localY}; setZoom(next);};
  const zoomValueRef = useRef(zoom), zoomActionRef = useRef(zoomAt); zoomValueRef.current = zoom; zoomActionRef.current = zoomAt;
  useEffect(() => {const viewport = viewportRef.current; if (!viewport) return; const handleWheel = (event: WheelEvent) => {event.preventDefault(); event.stopPropagation(); if (!event.deltaY) return; const next = clampZoom(zoomValueRef.current + (event.deltaY < 0 ? .2 : -.2)); zoomValueRef.current = next; zoomActionRef.current(next, event.clientX, event.clientY);}; viewport.addEventListener("wheel", handleWheel, {passive: false}); return () => viewport.removeEventListener("wheel", handleWheel);}, []);
  useLayoutEffect(() => {const viewport = viewportRef.current, anchor = zoomAnchorRef.current; if (!viewport || !anchor) return; viewport.scrollLeft = anchor.x * viewport.scrollWidth - anchor.localX; viewport.scrollTop = anchor.y * viewport.scrollHeight - anchor.localY; zoomAnchorRef.current = null;}, [zoom]);
  useLayoutEffect(() => {if (!focusTarget || !selectionKey) return; focusRequestRef.current = selectionKey; zoomAnchorRef.current = null; if (zoomValueRef.current < 1.75) {zoomValueRef.current = 1.75; setZoom(1.75);} }, [selectionKey, Boolean(focusTarget)]);
  useLayoutEffect(() => {if (!focusTarget || !selectionKey || focusRequestRef.current !== selectionKey || zoom < 1.75) return; let secondFrame = 0; const firstFrame = requestAnimationFrame(() => {secondFrame = requestAnimationFrame(() => {const viewport = viewportRef.current, marker = markerRef.current; if (!viewport || !marker || focusRequestRef.current !== selectionKey) return; const left = marker.offsetLeft - viewport.clientWidth / 2, top = marker.offsetTop - viewport.clientHeight / 2; viewport.scrollTo({left: Math.max(0, Math.min(left, viewport.scrollWidth - viewport.clientWidth)), top: Math.max(0, Math.min(top, viewport.scrollHeight - viewport.clientHeight)), behavior: "smooth"}); focusRequestRef.current = "";});}); return () => {cancelAnimationFrame(firstFrame); cancelAnimationFrame(secondFrame);};}, [selectionKey, zoom, imageSize?.[0], imageSize?.[1]]);
  useEffect(() => () => panCleanupRef.current(), []);
  const startPan = (event: any) => {if (event.button !== 0 || !viewportRef.current) return; event.preventDefault(); panCleanupRef.current(); didDragRef.current = false; dragRef.current = {x: event.clientX, y: event.clientY, startX: event.clientX, startY: event.clientY}; setPanning(true); const move = (next: MouseEvent) => {const drag = dragRef.current, viewport = viewportRef.current; if (!drag || !viewport) return; next.preventDefault(); if (Math.hypot(next.clientX - drag.startX, next.clientY - drag.startY) > 6) didDragRef.current = true; viewport.scrollLeft -= next.clientX - drag.x; viewport.scrollTop -= next.clientY - drag.y; drag.x = next.clientX; drag.y = next.clientY;}, stop = () => {window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", stop); dragRef.current = null; setPanning(false); panCleanupRef.current = () => {};}; window.addEventListener("mousemove", move, {passive: false}); window.addEventListener("mouseup", stop, {once: true}); panCleanupRef.current = stop;};
  const markerPoints = selectedPoint?.labelBounds || [], hitTargets = selectedPoint?.hitBounds || [], visualSize = visual?.size;
  return <section className="zoomMapCard">
    <header><div><b>{stableVariantTitle}</b><small>{activeVariant.primary ? "脱出地点・タスク・鍵位置の表示に対応" : `${activeVariant.kind} · 閲覧用（位置表示は基準版のみ）`}</small></div><div className="mapZoomControls"><button type="button" onClick={() => zoomAt(zoom - .25)} disabled={zoom <= 1} aria-label="マップを縮小">−</button><span>{Math.round(zoom * 100)}%</span><button type="button" onClick={() => zoomAt(zoom + .25)} disabled={zoom >= 3} aria-label="マップを拡大">＋</button><button type="button" onClick={resetView}>全体表示</button></div></header>
    <div className="mapVariantPicker"><div><b>マップ画像を切り替え</b><small>{variantsLoading ? "英語Wikiから別バージョンを確認中…" : `${variants.length}種類`}</small></div><div>{variants.map((variant, index) => {const ordinal=variant.primary?0:variants.slice(0,index+1).filter(item=>item.kind===variant.kind).length,label=variant.primary?"脱出地点対応マップ":`${selected.ja} · ${variant.kind}${ordinal>1?` ${ordinal}`:""}`;return <button type="button" className={variant.id === activeVariant.id ? "active" : ""} key={variant.id} onClick={() => setActiveVariantId(variant.id)} title={variant.title}><i>{String(index + 1).padStart(2, "0")}</i><span><b>{variant.primary ? "脱出地点対応" : variant.kind}</b><small>{label}</small></span>{variant.primary && <em>機能付き</em>}</button>})}</div></div>
    <div ref={viewportRef} className={`zoomMapViewport ${panning ? "panning" : ""}`} onMouseDown={startPan}><div className="zoomMapInner" style={{width: `${zoom * 100}%`}}><img draggable={false} src={imageUrl} onLoad={event => {setImageSize([event.currentTarget.naturalWidth, event.currentTarget.naturalHeight]); setImageError(false);}} onError={() => setImageError(true)} alt={`${selected.name} ${stableVariantTitle}`} />
      {visualSize && !imageError && <><svg className="mapAnnotationOverlay" viewBox={`0 0 ${visualSize[0]} ${visualSize[1]}`} preserveAspectRatio="none" aria-hidden="true"><defs><filter id={`annotation-glow-${selected.id || printedLabelKey(selected.name)}`} x="-50%" y="-50%" width="200%" height="200%"><feGaussianBlur stdDeviation="8" result="blur"/><feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>{auditAnnotations.flatMap((annotation, annotationIndex) => annotation.labelBounds.map((rect, rectIndex) => <rect className="mapAuditRect" key={`audit-${annotationIndex}-${rectIndex}`} x={rect.x} y={rect.y} width={rect.width} height={rect.height} vectorEffect="non-scaling-stroke"/>))}{markerPoints.map((rect, index) => <rect className="printedLabelFrame" key={`${selectionKey}-frame-${index}`} x={rect.x} y={rect.y} width={rect.width} height={rect.height} rx="5" vectorEffect="non-scaling-stroke" filter={`url(#annotation-glow-${selected.id || printedLabelKey(selected.name)})`}/>)}</svg>{focusTarget && <span ref={markerRef} className="mapFocusAnchor" style={{left: `${focusTarget.left}%`, top: `${focusTarget.top}%`}} />}{hitTargets.map((rect, index) => <button key={`${selectionKey}-hit-${index}`} type="button" className="annotationHitTarget" style={pixelRectStyle(rect, visualSize)} onClick={event => {event.stopPropagation(); if (event.detail === 0 || !didDragRef.current) onShowDetails();}} aria-label={`${selectedExtract.name}の現地画像と詳細を見る${index ? ` ${index + 1}` : ""}`} title="クリックして現地画像と詳細を見る" />)}{keyPositions.map(point=><span className="keyLocationMarker" key={`${keyFocus?.keyId}-${point.index}`} style={{left:`${point.left}%`,top:`${point.top}%`}} title={`${keyFocus?.keyName} 使用位置 ${point.index+1}`}><i>KEY</i><b>{point.index+1}</b></span>)}</>}
    </div></div>
    {imageError && <div className="mapImageError"><b>マップ画像を表示できません</b><span>通信を確認するか、保存済み画像を利用してください。</span></div>}
    {selectedExtract && <div className="extractMapPreview" aria-live="polite"><div><small>選択中の脱出地点</small><strong>{selectedExtract.name}</strong><span>{!activeVariant.primary ? "脱出地点対応マップへ戻すと強調表示できます" : selectedPoint ? "文字を囲む枠をクリックすると詳細を確認できます" : !sizeMatches ? `画像版が異なります（対応版 ${visual?.revision || "未登録"}）` : selectedLabelNotPrinted ? "この画像には名称が印字されていないため、誤った枠は表示しません" : "この画像版の注釈を確認できません"}</span></div>{!activeVariant.primary && <button type="button" onClick={() => setActiveVariantId("extraction")}>脱出地点対応マップへ戻す</button>}</div>}
    {keyFocus && <div className="keyMapPreview" aria-live="polite"><div><small>鍵の使用場所</small><strong>{keyFocus.keyName}</strong><span>{!activeVariant.primary?"位置表示は基準版マップで確認できます":keyPositions.length?`${keyPositions.length}箇所をKEYマーカーで表示しています（API座標）`:"このマップの位置座標は登録されていません"}</span></div>{!activeVariant.primary&&<button type="button" onClick={()=>setActiveVariantId("extraction")}>基準版マップへ戻す</button>}</div>}
    <footer><div><span>ドラッグで移動</span><span>ホイール上で拡大</span><span>ホイール下で縮小</span>{visual && <span>画像版 {visual.revision}</span>}{activeVariant.width&&activeVariant.height&&<span>{activeVariant.width} × {activeVariant.height}</span>}{imageHash && <span title={imageHash}>SHA-256 {imageHash.slice(0, 10)}…</span>}{activeVariant.primary ? <a href="https://reemr.se/" target="_blank" rel="noreferrer">MAP: RE3MR ↗</a> : variantsSourceUrl && <a href={variantsSourceUrl} target="_blank" rel="noreferrer">英語WikiのMaps節 ↗</a>}</div><div className={`offlineSaveState ${cached ? "saved" : ""}`}><button type="button" disabled={saving} onClick={saveOffline}>{cached ? "↻ オフラインデータを更新" : "↓ このマップをオフライン保存"}</button>{message && <small>{message}</small>}</div></footer>
  </section>;
}

function EnhancedMapHub() {
  const restored = useMemo(() => {if (typeof window === "undefined") return {} as any; try {return JSON.parse(sessionStorage.getItem("tarkov-map-last-view") || "{}");} catch {return {} as any;} }, []), initialCollapsed = {PMC: true, Scav: true, All: true, Transit: true};
  const [selected, setSelected] = useState<any>(() => mapStageFor(history.state?.mapStage || restored.selected) || null), [keyFocus, setKeyFocus] = useState<KeyMapFocus | null>(() => history.state?.mapKeyFocus || null), [extract, setExtract] = useState<any>(null), [detailOpen, setDetailOpen] = useState(false), [extractGuide, setExtractGuide] = useState<any>(null), [guideLoading, setGuideLoading] = useState(false), [latest, setLatest] = useState<any[]>([]), [loading, setLoading] = useState(true), [mapDataStatus, setMapDataStatus] = useState<{source: string; updatedAt?: string | null; error?: string;}>({source: "読み込み中"}), [collapsedGroups, setCollapsedGroups] = useState<Record<string, boolean>>({...initialCollapsed, ...(restored.collapsedGroups || {})});
  useEffect(() => {let cancelled = false; const normalize = (value: MapDataResult | any[]) => Array.isArray(value) ? {maps: value, source: "従来キャッシュ", updatedAt: null} : value; const apply = (value: MapDataResult | any[], finishLoading = false) => {if (cancelled) return; const result = normalize(value); if (Array.isArray(result.maps) && result.maps.length) setLatest(result.maps); setMapDataStatus({source: result.source || "不明", updatedAt: result.updatedAt, error: result.error}); if (finishLoading) setLoading(false);}; const load = async () => {try {apply({maps: bundledMapData.maps, source: "同梱", updatedAt: bundledMapData.updatedAt}, true); if (!window.stashAI?.maps) return; apply(await window.stashAI.maps()); window.stashAI.refreshMaps?.().then(value => apply(value)).catch(() => {});} catch {setMapDataStatus(old => ({...old, error: "オンライン更新に失敗したため同梱データを表示しています"}));} finally {if (!cancelled) setLoading(false);}}; load(); return () => {cancelled = true;};}, []);
  useEffect(() => {sessionStorage.setItem("tarkov-map-last-view", JSON.stringify({selected: selected?.name || "", collapsedGroups}));}, [selected, collapsedGroups]);
  useEffect(() => {const restore = (event: PopStateEvent) => {if (event.state?.stashPage === "maps") {setSelected(mapStageFor(event.state?.mapStage) || null); setKeyFocus(event.state?.mapKeyFocus || null); setExtract(null);} }; window.addEventListener("popstate", restore); return () => window.removeEventListener("popstate", restore);}, []);
  useEffect(() => {let cancelled = false; setExtractGuide(null); if (detailOpen && extract && selected && window.stashAI?.extractGuide) {setGuideLoading(true); window.stashAI.extractGuide(selected.name, extract.name).then(guide => {if (!cancelled) setExtractGuide(guide);}).catch(() => {if (!cancelled) setExtractGuide(null);}).finally(() => {if (!cancelled) setGuideLoading(false);});} return () => {cancelled = true;};}, [detailOpen, extract?.name, selected?.name]);
  const openStage = (stage: any) => {history.pushState({...history.state, stashPage: "maps", mapStage: stage.name, mapKeyFocus: null}, ""); setSelected(stage); setKeyFocus(null); setExtract(null); setDetailOpen(false); setCollapsedGroups(initialCollapsed);};
  const mapKey = (value: any) => String(value || "").toLowerCase().replace(/^the\s+/, "").replace(/[^a-z0-9]/g, "");
  const source = selected && (latest.find(x => String(x.id || "") === String(selected.id || "__missing__")) || latest.find(x => sameMapLocation(x, selected))), liveExtracts = source?.extracts || [], selectedMapKey = mapKey(mapLocation(selected).name), manualExtracts = selectedMapKey === "factory" ? factoryExtracts : selectedMapKey === "lab" ? labExtracts : selectedMapKey === "labyrinth" ? labyrinthExtracts : null, extracts = manualExtracts ? manualExtracts.map(manual => ({...liveExtracts.find((live: any) => mapKey(live.name) === mapKey(manual.name)), ...manual})) : liveExtracts, transits = selectedMapKey === "factory" ? [] : (source?.transits || []), groups = [{key: "PMC", title: "PMC専用", className: "pmc", items: extracts.filter((x: any) => /^pmc$/i.test(x.faction))}, {key: "Scav", title: "SCAV専用", className: "scav", items: extracts.filter((x: any) => /scav/i.test(x.faction))}, {key: "All", title: "PMC・SCAV共通", className: "both", items: extracts.filter((x: any) => /all|both/i.test(x.faction))}, {key: "Transit", title: "他マップへのトランジット", className: "transit", items: transits}];
  if (selected) return <section className="mapHub mapDetail"><button className="mapBack" onClick={() => {setSelected(null); setKeyFocus(null); setExtract(null);}}>← 全マップへ戻る</button><header><div><small>ZOOMABLE MAP GUIDE</small><h2>{selected.ja}<span>{selected.name}</span></h2><p>{selected.desc}</p></div><div><b>{loading ? "脱出地点データを準備中" : mapDataStatus.error && !latest.length ? "脱出地点データを利用できません" : `${mapDataStatus.source}データを表示中`}</b>{mapDataStatus.updatedAt && <small>更新 {new Date(mapDataStatus.updatedAt).toLocaleString("ja-JP")}</small>}<small>{source?.players && `PMC ${source.players}`}</small><small>{source?.raidDuration && `制限時間 ${source.raidDuration}分`}</small></div></header><div className="mapInfoLayout"><div><OfflineZoomMap selected={selected} selectedExtract={extract} keyFocus={keyFocus} onShowDetails={() => setDetailOpen(true)} /><div className="mapBeginnerTips"><b>初心者向け確認ポイント</b><span>出撃地点を確認したら、最初に大きな建物・道路・地形から現在地を特定してください。</span><span>脱出地点はレイド中にOキーを2回押して、利用可能な名称を確認してください。</span></div></div><aside className="extractPanel grouped"><h3>脱出地点 <small>{extracts.length}か所{transits.length ? ` ＋ トランジット${transits.length}か所` : ""}</small></h3><div className="extractLegend"><span className="pmc">PMC専用</span><span className="scav">SCAV専用</span><span className="both">共通</span></div>{loading && selected.name !== "Factory" && <p>保存済みデータを読み込み中…</p>}{groups.map(group => group.items.length ? <section className={`extractGroup ${group.className} ${collapsedGroups[group.key] ? "collapsed" : ""}`} key={group.key}><button type="button" className="extractGroupToggle" aria-expanded={!collapsedGroups[group.key]} onClick={() => setCollapsedGroups(old => ({...old, [group.key]: !old[group.key]}))}><b>{group.title}</b><span>{group.items.length}か所</span><i>{collapsedGroups[group.key] ? "＋" : "−"}</i></button>{!collapsedGroups[group.key] && group.items.map((point: any, index: number) => <button key={`${point.name}-${index}`} className={extract === point ? "selected" : ""} onClick={() => {setKeyFocus(null); setExtract(point); setDetailOpen(false);}}><i></i><span><strong>{point.name}</strong><small>{point.direction || point.requirement || point.faction}</small></span><b>›</b></button>)}</section> : null)}{!loading && selected.name !== "Factory" && !liveExtracts.length && <p className="mapDataError">脱出地点データを取得できませんでした。アプリの同梱データも確認してください。</p>}</aside></div>{detailOpen && extract && <div className="extractModal detailDrawer" onClick={() => setDetailOpen(false)}><article onClick={event => event.stopPropagation()}><button onClick={() => setDetailOpen(false)} aria-label="詳細を閉じる">×</button><small>{extract.kind === "transit" ? "TRANSIT POINT" : "EXTRACTION POINT"} · {extract.faction}</small><h2>{extract.name}</h2><div className="extractGuideImages">{[...(extract.image ? [{url: extract.image, caption: `${extract.name} 現地画像`}] : []), ...(extractGuide?.images || [])].filter((image: any, index: number, list: any[]) => list.findIndex(x => x.url === image.url) === index).slice(0, 4).map((image: any, index: number) => <a href={image.url} target="_blank" rel="noreferrer" key={`${image.url}-${index}`}><i>{String(index + 1).padStart(2, "0")}</i><img src={image.url} alt={`${extract.name}の現地参考画像 ${index + 1}`} /><span>{image.caption || `${extract.name} 参考画像 ${index + 1}`}<small>原寸表示</small></span></a>)}</div>{guideLoading && <p className="extractGuideLoading">現地画像をネットから検索しています…</p>}<div className="extractGuideLinks">{extractGuide?.sourceUrl && <a href={extractGuide.sourceUrl} target="_blank" rel="noreferrer">Wikiで確認 ↗</a>}{extractGuide?.searchUrl && <a href={extractGuide.searchUrl} target="_blank" rel="noreferrer">現地画像を検索 ↗</a>}</div> </article></div>}</section>;
  return <section className="mapHub"><header className="mapHubTitle"><small>MAP INTELLIGENCE</small><h2>マップを選択</h2><p>全体図のマップ名を囲む枠から選択してください。</p></header><div className="worldMap interactiveWorld"><img src={asset("maps/world-select.png")} alt="Tarkov地域の全体図" />{stageHotspots.map(hotspot => {const stage = mapStages.find(x => x.name === hotspot.name)!; return <button className="stageHotspot" aria-label={`${stage.ja}を開く`} title={`${stage.ja}の情報を見る`} key={hotspot.name} style={{left: `${hotspot.left}%`, top: `${hotspot.top}%`, width: `${hotspot.width}%`, height: `${hotspot.height}%`}} onClick={() => openStage(stage)}><span>{stage.name}</span></button>;})}</div><div className="additionalMaps"><span>全体図に記載のないマップ</span>{mapStages.filter(stage => !stageHotspots.some(x => x.name === stage.name)).map(stage => <button key={stage.name} onClick={() => openStage(stage)}>{stage.name}</button>)}</div></section>;
}



function taskLocations(task: any) {const entries = [...(task.map ? [{id: task.mapId, name: task.map, nameJa: task.mapJa}] : []), ...(task.objectives || []).flatMap((objective: any) => objective.maps || [])]; return normalizeMapEntries(entries).filter((map: any) => map.name).map((map: any) => ({name: map.name, label: map.label, id: map.id})) as Array<{name: string; label: string; id: string;}>;}
function taskLocationLabel(task: any) {const locations = taskLocations(task); return locations.length === 0 ? "ANY MAP" : locations.length === 1 ? locations[0].label : `複数マップ（${locations.length}）`;}
function objectiveMapsFor(task: any, objective: any) {if (Array.isArray(objective.maps) && objective.maps.length) return normalizeMapEntries(objective.maps); if (!task.map) return []; const stage = mapStageFor({id: task.mapId, name: task.map, nameJa: task.mapJa}); return normalizeMapEntries([{id: stage?.id || task.mapId || "", name: task.map, nameJa: task.mapJa || task.map, slug: String(task.map).toLowerCase().replace(/[^a-z0-9]+/g, "-")}]);}
function taskMapReferences(task: any) {const entries = [...(task.objectives || []).flatMap((objective: any) => objective.maps || []), ...(task.map ? [{id: task.mapId, name: task.map, nameJa: task.mapJa}] : [])]; return normalizeMapEntries(entries).filter((map: any) => map.name);}
function TaskGuidePage() {
  const restored = useMemo(() => {if (typeof window === "undefined") return {} as any; let saved: any = {}; try {saved = JSON.parse(sessionStorage.getItem("tarkov-task-last-view") || "{}");} catch {} const current = history.state; return current?.stashPage === "guide" && current?.guideView ? {...saved, guideView: current.guideView, selected: current.guideSelected || "", selectedTrader: current.guideTrader || ""} : saved;}, []);
  const [section, setSection] = useState<"story" | "tasks" | "battle">((history.state?.guideSection === "tasks" ? "tasks" : (["story", "tasks", "battle"].includes(restored.section) ? restored.section : "tasks")) as any), [search, setSearch] = useState(String(restored.search || "").slice(0, 100)), [selected, setSelected] = useState(String(restored.selected || "")), [selectedTrader, setSelectedTrader] = useState(String(restored.selectedTrader || "")), [guideView, setGuideView] = useState<"traders" | "directory" | "list" | "detail">((["traders", "directory", "list", "detail"].includes(restored.guideView) ? restored.guideView : "traders") as any), [taskSort, setTaskSort] = useState<"recommended" | "level" | "name">(restored.taskSort || "recommended"), [taskGroup, setTaskGroup] = useState<"none" | "map" | "trader">(restored.taskGroup || "none"), [mapFilter, setMapFilter] = useState(String(restored.mapFilter || "all")), [guide, setGuide] = useState<any>(() => normalizeTaskGuide(taskGuide)), [portraits, setPortraits] = useState<Record<string, string>>({}), [updating, setUpdating] = useState(false), [updateMessage, setUpdateMessage] = useState("");
  useEffect(() => {sessionStorage.setItem("tarkov-task-last-view", JSON.stringify({section, search: search.slice(0, 100), selected, selectedTrader, guideView, taskSort, taskGroup, mapFilter}));}, [section, search, selected, selectedTrader, guideView, taskSort, taskGroup, mapFilter]);
  useEffect(() => {const restore = (event: PopStateEvent) => {if (event.state?.stashPage !== "guide" || !event.state?.guideView) return; setGuideView(event.state.guideView); setSelected(event.state.guideSelected || ""); setSelectedTrader(event.state.guideTrader || ""); setSearch("");}; window.addEventListener("popstate", restore); return () => window.removeEventListener("popstate", restore);}, []);
  useEffect(() => {try {const cached = localStorage.getItem("tarkov-task-guide-cache"); if (cached) {const normalized = normalizeTaskGuide(JSON.parse(cached)); setGuide(normalized); localStorage.setItem("tarkov-task-guide-cache", JSON.stringify(normalized));} } catch {} }, []);
  useEffect(() => {const names = ["Prapor", "Therapist", "Fence", "Skier", "Peacekeeper", "Mechanic", "Ragman", "Jaeger", "Ref"]; let cached: Record<string, string> = {}; try {cached = JSON.parse(localStorage.getItem("tarkov-trader-portraits") || "{}"); setPortraits(cached);} catch {} if (window.stashAI?.traderPortraits) window.stashAI.traderPortraits(names).then(found => {const next = {...cached, ...found}; setPortraits(next); localStorage.setItem("tarkov-trader-portraits", JSON.stringify(next));}).catch(() => {});}, []);
  const refresh = async () => {
    setUpdating(true); setUpdateMessage("最新データを取得しています…"); try {
      const base = "https://json.tarkov.dev/regular/", names = ["tasks", "tasks_en", "tasks_ja", "traders", "traders_en", "maps", "maps_en", "maps_ja", "items", "items_en", "items_ja"], docs = await Promise.all(names.map(async name => {const response = await fetch(base + name); if (!response.ok) throw Error(`${name}: ${response.status}`); return response.json();})), [raw, en, ja, traderDoc, traderEn, mapDoc, mapEn, mapJa, itemDoc, itemEn, itemJa] = docs, traderMap = traderDoc.data.traders || traderDoc.data, mapMap = mapDoc.data.maps || mapDoc.data, itemMap = itemDoc.data.items || itemDoc.data, oldById = new Map(guide.tasks.map((x: any) => [x.id, x])), tr = (table: any, key: string, fallback = "") => table?.data?.[key] || fallback;
      const tradersById = (id: string) => {const t = traderMap[id] || Object.values(traderMap).find((x: any) => x.id === id) as any; return tr(traderEn, t?.name || `${id} name`, t?.normalizedName || "Other");}, mapNames = (id: string) => {const m = mapMap[id] || Object.values(mapMap).find((x: any) => x.id === id) as any; return {id, name: tr(mapEn, m?.name || `${id} Name`, m?.normalizedName || ""), nameJa: tr(mapJa, m?.name || `${id} Name`, "")};};
      const itemInfo = (value: any) => {const id = String(typeof value === "string" ? value : value?.id || value?.item || ""); const rawItem: any = itemMap[id] || Object.values(itemMap).find((entry: any) => entry.id === id) || {}; return {id, name: tr(itemEn, `${id} Name`, rawItem.name || rawItem._name || id), nameJa: tr(itemJa, `${id} Name`, ""), shortName: tr(itemEn, `${id} ShortName`, rawItem.shortName || rawItem._shortName || id), image: `https://assets.tarkov.dev/${id}-grid-image.webp`};};
      const tasks = Object.values(raw.data.tasks).map((task: any) => {const old: any = oldById.get(task.id) || {}, taskMap = mapNames(task.map), oldObjectives = new Map((old.objectives || []).map((objective: any) => [objective.id, objective])); return applyLiveTaskCorrections({...old, id: task.id, name: tr(en, `${task.id} name`, task.name), nameJa: tr(ja, `${task.id} name`, ""), trader: tradersById(task.trader), level: task.minPlayerLevel || 1, image: task.taskImageLink || old.image || "", wiki: task.wikiLink || old.wiki || "", map: taskMap.name, mapJa: taskMap.nameJa, mapId: taskMap.id, prerequisites: (task.taskRequirements || []).map((r: any) => ({id: r.task, name: tr(en, `${r.task} name`, r.task), nameJa: tr(ja, `${r.task} name`, ""), status: r.status || []})), objectives: (task.objectives || []).map((o: any) => {const oldObjective: any = oldObjectives.get(o.id) || {}; return {id: o.id, type: o.type, description: tr(en, o.description, tr(en, o.id, o.type)), descriptionJa: tr(ja, o.description, tr(ja, o.id, "")), count: o.count || 1, foundInRaid: !!o.foundInRaid, maps: (o.maps || []).map(mapNames), zones: Array.isArray(o.zones) && o.zones.length ? o.zones : (oldObjective.zones || []), items: (o.items || []).slice(0, 12).map(itemInfo)};})});});
      const next = normalizeTaskGuide({...guide, dataSource: "json.tarkov.dev + game-confirmed corrections", updatedAt: new Date().toISOString(), tasks}); setGuide(next); localStorage.setItem("tarkov-task-guide-cache", JSON.stringify(next)); setUpdateMessage(`${tasks.length}件を更新しました（ゲーム内確認済み補正を適用）`);
    } catch (error) {console.error(error); setUpdateMessage("更新できませんでした。インターネット接続を確認してください。");} finally {setUpdating(false);}
  };
  const guideTraderOrder = ["Prapor", "Therapist", "Fence", "Skier", "Peacekeeper", "Mechanic", "Ragman", "Jaeger", "Ref"], traderFace: Record<string, string> = {Prapor: asset("traders/faces/prapor.png"), Therapist: asset("traders/faces/therapist.png"), Fence: asset("traders/faces/fence.png"), Skier: asset("traders/faces/skier.png"), Peacekeeper: asset("traders/faces/peacekeeper.png"), Mechanic: asset("traders/faces/mechanic.png"), Ragman: asset("traders/faces/ragman.png"), Jaeger: asset("traders/faces/jaeger.png"), Ref: "https://tarkov.dev/images/traders/ref.jpg"}, q = normalizeSearchText(search), matches = (value: string) => !q || normalizeSearchText(value).includes(q), searched = guide.tasks.filter((task: any) => matches(`${task.nameJa || ""} ${task.name || ""} ${task.trader || ""} ${taskLocations(task).map(map => `${map.name} ${map.label}`).join(" ")}`)), mapOptions = [...new Map(guide.tasks.flatMap((task: any) => taskLocations(task)).map((map: any) => [map.id || map.name, {value: map.id || map.name, label: map.label}])).values()].sort((a: any, b: any) => a.label.localeCompare(b.label, "ja")), filterAndSort = (list: any[]) => [...list].filter(task => {const locations = taskLocations(task); return mapFilter === "all" || (mapFilter === "any" ? !locations.length : locations.some(map => map.id === mapFilter || map.name === mapFilter));}).sort((a, b) => {const byName = String(a.name || "").localeCompare(String(b.name || ""), "en", {sensitivity: "base"}); if (taskSort === "name") return byName; const byLevel = Number(a.level || 0) - Number(b.level || 0); if (taskSort === "level") return byLevel || byName; const byPrerequisite = (a.prerequisites?.length || 0) - (b.prerequisites?.length || 0), byTrader = guideTraderOrder.indexOf(a.trader) - guideTraderOrder.indexOf(b.trader); return byLevel || byPrerequisite || byTrader || byName;}), visibleTasks = filterAndSort(searched.filter((task: any) => !selectedTrader || task.trader === selectedTrader)), directoryTasks = filterAndSort(searched), active = guide.tasks.find((task: any) => task.id === selected);
  const openGuide = (view: "traders" | "directory" | "list" | "detail", taskId = selected, trader = selectedTrader) => {history.pushState({...history.state, stashPage: "guide", guideView: view, guideSelected: taskId, guideTrader: trader}, ""); setGuideView(view); setSelected(taskId); setSelectedTrader(trader);};
  const searchCount = section === "tasks" ? searched.length : section === "story" ? guide.story.filter((entry: any) => matches(entry.name)).length : guide.battlePass.filter((entry: any) => matches(entry.name)).length;
  return <section className="manager taskGuide"><div className="guideHeader"><div><span>04 / TASK INTELLIGENCE</span><h2>タスク情報</h2><p>目的・準備・場所・報酬をひとつの画面で確認。</p></div><div className="guideUpdate"><small>最終更新 {new Date(guide.updatedAt).toLocaleString("ja-JP")}</small><button onClick={refresh} disabled={updating}>{updating ? "更新中…" : "↻ 最新情報に更新"}</button>{updateMessage && <em>{updateMessage}</em>}</div></div><div className="guideTabs"><button className={section === "story" ? "active" : ""} onClick={() => setSection("story")}>ストーリータスク</button><button className={section === "tasks" ? "active" : ""} onClick={() => setSection("tasks")}>サブタスク</button><button className={section === "battle" ? "active" : ""} onClick={() => setSection("battle")}>バトルパス</button></div><label className="questHomeSearch taskGlobalSearch"><span>⌕</span><input value={search} onChange={e => setSearch(e.target.value)} placeholder="タスク名・トレーダー名で検索…" autoComplete="off" /><b>{search ? `${searchCount}件` : "タスク内を検索"}</b>{search && <button type="button" onClick={() => setSearch("")}>×</button>}</label>
    {section === "tasks" && <section className="questBrowser">{search ? <>
      <TaskDirectoryControls mode={guideView} onMode={mode => openGuide(mode, "", "")} sort={taskSort} onSort={setTaskSort} group={taskGroup} onGroup={setTaskGroup} mapFilter={mapFilter} onMapFilter={setMapFilter} maps={mapOptions} />
      <GuideTaskList title="検索結果" subtitle="すべてのトレーダー" tasks={directoryTasks} groupBy={taskGroup} sort={taskSort} onSelect={task => {setSearch(""); openGuide("detail", task.id, task.trader);}} />
    </> : <>
      {guideView === "traders" && <><TaskDirectoryControls mode={guideView} onMode={mode => openGuide(mode, "", "")} sort={taskSort} onSort={setTaskSort} group={taskGroup} onGroup={setTaskGroup} mapFilter={mapFilter} onMapFilter={setMapFilter} maps={mapOptions} /><div className="traderPortraitGrid">{guideTraderOrder.map(trader => {const traderTasks = guide.tasks.filter((task: any) => task.trader === trader), portrait = traderFace[trader] || portraits[trader]; return <button key={trader} onClick={() => {setSearch(""); openGuide("list", "", trader);}}><div>{portrait ? <img src={portrait} alt={`${trader}の顔写真`} /> : <span className="portraitLoading">肖像<br />読込中</span>}<i>{traderTasks.length}</i></div><strong>{trader}</strong><small>タスク一覧を見る　→</small></button>;})}</div></>}
      {guideView === "directory" && <><TaskDirectoryControls mode={guideView} onMode={mode => openGuide(mode, "", "")} sort={taskSort} onSort={setTaskSort} group={taskGroup} onGroup={setTaskGroup} mapFilter={mapFilter} onMapFilter={setMapFilter} maps={mapOptions} /><GuideTaskList title="すべてのサブタスク" subtitle="マップ・トレーダーを横断して表示" tasks={directoryTasks} groupBy={taskGroup} sort={taskSort} onSelect={task => openGuide("detail", task.id, task.trader)} /></>}
      {guideView === "list" && <><GuideBreadcrumb onBack={() => history.back()} onHome={() => {setSearch(""); openGuide("traders", "", "");}} label="ひとつ前へ戻る" title={selectedTrader} /><TaskDirectoryControls mode={guideView} onMode={mode => openGuide(mode, "", "")} sort={taskSort} onSort={setTaskSort} group={taskGroup} onGroup={setTaskGroup} mapFilter={mapFilter} onMapFilter={setMapFilter} maps={mapOptions} /><GuideTaskList title={`${selectedTrader}のタスク`} subtitle="指定条件で表示" tasks={visibleTasks} groupBy={taskGroup} sort={taskSort} onSelect={task => openGuide("detail", task.id, selectedTrader || task.trader)} /></>}
      {guideView === "detail" && active && <><GuideBreadcrumb onBack={() => {setSearch(""); selectedTrader ? openGuide("list", "", selectedTrader) : openGuide("directory", "", "");}} onHome={() => {setSearch(""); openGuide("traders", "", "");}} label={selectedTrader ? `${selectedTrader}の一覧へ戻る` : "タスク一覧へ戻る"} title={active.name} /><TaskDetail task={active} /></>}
    </>}</section>}
    {section === "story" && <div className="storyGuideGrid">{guide.story.filter((x: any) => matches(x.name)).map((story: any) => <article key={story.id}><div className="chapterNo">CHAPTER {String(story.chapter).padStart(2, "0")}</div><h3>{story.name}</h3><p>{story.summary}</p><a href={story.source} target="_blank" rel="noreferrer">情報源を開く ↗</a></article>)}</div>}
    {section === "battle" && <div className="battleGuide">{guide.battlePass.filter((x: any) => matches(x.name)).map((pass: any) => <article key={pass.id}><div className="battleVisual"><span>BATTLE<br />PASS</span><b>{pass.season}</b></div><div><small>{pass.season}</small><h3>{pass.name}</h3><p>{pass.summary}</p><ol>{pass.objectives.map((objective: string) => <li key={objective}>{objective}</li>)}</ol><a href={pass.source} target="_blank" rel="noreferrer">シーズン情報を確認 ↗</a></div></article>)}</div>}
  </section>;
}
function GuideBreadcrumb({onBack, onHome, label, title}: {onBack: () => void, onHome?: () => void, label: string, title: string;}) {return <div className="guideBreadcrumb"><div className="guideNavButtons"><button type="button" onClick={onBack}><span>←</span>{label}</button>{onHome && <button type="button" className="guideHomeButton" onClick={onHome}><span>⌂</span>タスクTOPへ戻る</button>}</div><div className="guideCurrent"><small>CURRENT PAGE</small><strong>{title}</strong></div></div>;}
function TaskDirectoryControls({mode, onMode, sort, onSort, group, onGroup, mapFilter, onMapFilter, maps}: {mode: string; onMode: (mode: "traders" | "directory") => void; sort: "recommended" | "level" | "name"; onSort: (value: any) => void; group: "none" | "map" | "trader"; onGroup: (value: any) => void; mapFilter: string; onMapFilter: (value: string) => void; maps: Array<{value: string; label: string;}>;}) {return <section className="taskDirectoryControls" aria-label="サブタスクの表示設定"><div className="taskViewSwitch"><button type="button" className={mode === "traders" ? "active" : ""} onClick={() => onMode("traders")}>トレーダーから探す</button><button type="button" className={mode !== "traders" ? "active" : ""} onClick={() => onMode("directory")}>全タスクから探す</button></div><label><span>並べ替え</span><select value={sort} onChange={event => onSort(event.target.value)}><option value="recommended">推奨順</option><option value="level">レベル順</option><option value="name">名前順 A–Z</option></select></label><label><span>グループ</span><select value={group} onChange={event => onGroup(event.target.value)}><option value="none">グループなし</option><option value="map">マップ別</option><option value="trader">トレーダー別</option></select></label><label><span>マップ</span><select value={mapFilter} onChange={event => onMapFilter(event.target.value)}><option value="all">すべてのマップ</option><option value="any">場所指定なし（ANY MAP）</option>{maps.map(map => <option key={map.value} value={map.value}>{map.label}</option>)}</select></label></section>;}
function GuideTaskList({title, subtitle, tasks, groupBy, sort, onSelect}: {title: string; subtitle: string; tasks: any[]; groupBy: "none" | "map" | "trader"; sort: "recommended" | "level" | "name"; onSelect: (task: any) => void;}) {const groups = groupBy === "none" ? [["", tasks]] : [...tasks.reduce((result: Map<string, any[]>, task: any) => {const key = groupBy === "map" ? (taskLocations(task).length ? taskLocationLabel(task) : "場所指定なし（ANY MAP）") : (task.trader || "トレーダー不明"), current = result.get(key) || []; current.push(task); result.set(key, current); return result;}, new Map<string, any[]>()).entries()].sort(([a], [b]) => {if (a.includes("ANY MAP")) return 1; if (b.includes("ANY MAP")) return -1; return a.localeCompare(b, "ja");}), badge = sort === "name" ? "A → Z" : sort === "level" ? "LEVEL" : "RECOMMENDED", cards = (list: any[]) => <div className="guideTaskCards">{list.map(task => <button key={task.id} onClick={() => onSelect(task)}><span>LV.{task.level}</span><div><strong>{task.name}</strong><em>{task.trader} · {taskLocationLabel(task)}</em></div><i>›</i></button>)}</div>; return <section className="guideTaskDirectory"><header><div><small>TASK DIRECTORY</small><h2>{title}</h2><p>{subtitle} · {tasks.length}件</p></div><b>{badge}</b></header>{tasks.length ? <div className="guideTaskGroups">{groups.map(([name, list]: any) => <section key={name || "all"}>{name && <header><strong>{name}</strong><span>{list.length}件</span></header>}{cards(list)}</section>)}</div> : <div className="guideEmpty">該当するタスクがありません。マップ条件を変更してください。</div>}</section>;}
function TaskDetail({task}: {task: any;}) {
  const objectiveLabel: Record<string, string> = {giveItem: "納品", findItem: "発見", shoot: "討伐", extract: "脱出", visit: "探索", mark: "設置", plantItem: "設置", buildItem: "組立", useItem: "使用"};
  const [media, setMedia] = useState<TaskMedia[]>([]), [mediaLoading, setMediaLoading] = useState(false), [lightbox, setLightbox] = useState<TaskMedia | null>(null), [mapView, setMapView] = useState<any>(null), [translations, setTranslations] = useState<Record<string, string>>({}), [translating, setTranslating] = useState(false), [requiredKeys, setRequiredKeys] = useState<any[]>([]), [requirementsLoading, setRequirementsLoading] = useState(true), [requirementsVerified, setRequirementsVerified] = useState(false), [weaponBuilds, setWeaponBuilds] = useState<any[]>([]), [weaponBuildLoading, setWeaponBuildLoading] = useState(false), [weaponBuildVerified, setWeaponBuildVerified] = useState(false);
  const hasJapanese = (value: string) => /[ぁ-んァ-ヶ一-龠々]/.test(value || ""), jp = (localized: string, english: string) => hasJapanese(localized) ? localized : translations[english] || localized || english;
  useEffect(() => {let cancelled = false; setMedia([]); setMediaLoading(!!task.wiki); if (window.stashAI && task.wiki) window.stashAI.media(task.wiki).then(images => {if (!cancelled) setMedia(selectTaskReferenceImages(images, task));}).catch(() => {if (!cancelled) setMedia([]);}).finally(() => {if (!cancelled) setMediaLoading(false);}); else setMediaLoading(false); return () => {cancelled = true;};}, [task.id, task.wiki, task.name]);
  useEffect(() => {let cancelled = false; setRequiredKeys([]); setRequirementsLoading(true); setRequirementsVerified(false); if (window.stashAI?.requirements) window.stashAI.requirements(task.id).then(result => {if (cancelled) return; const validKeys = (result.keys || []).filter((key: any) => key?.id && key.name && key.name !== key.id && key.shortName !== key.id); setRequiredKeys(validKeys); setRequirementsVerified(!!result.verified);}).catch(() => {}).finally(() => {if (!cancelled) setRequirementsLoading(false);}); else setRequirementsLoading(false); return () => {cancelled = true;};}, [task.id]);
  useEffect(() => {let cancelled = false; const hasBuild = (task.objectives || []).some((objective: any) => objective.type === "buildWeapon" || objective.type === "buildItem"); setWeaponBuilds([]); setWeaponBuildVerified(false); if (!hasBuild) {setWeaponBuildLoading(false); return () => {cancelled = true;};} setWeaponBuildLoading(true); window.stashAI?.weaponBuild(task.id).then(result => {if (cancelled) return; setWeaponBuilds(result.builds || []); setWeaponBuildVerified(!!result.verified);}).catch(() => {}).finally(() => {if (!cancelled) setWeaponBuildLoading(false);}); return () => {cancelled = true;};}, [task.id]);
  useEffect(() => {let cancelled = false; let cached: Record<string, string> = {}; try {cached = JSON.parse(localStorage.getItem("tarkov-ja-translations") || "{}");} catch {} setTranslations(cached); const candidates = [...(task.objectives || []).map((x: any) => x.description)].filter((text: string, index: number, list: string[]) => text && !hasJapanese(text) && list.indexOf(text) === index && !cached[text]); if (candidates.length && window.stashAI?.translate) {setTranslating(true); window.stashAI.translate(candidates).then(result => {if (cancelled) return; const usable = Object.fromEntries(Object.entries(result).filter(([en, ja]) => ja && ja !== en && hasJapanese(ja))); const next = {...cached, ...usable}; setTranslations(next); localStorage.setItem("tarkov-ja-translations", JSON.stringify(next));}).catch(() => {}).finally(() => {if (!cancelled) setTranslating(false);});} return () => {cancelled = true;};}, [task.id]);
  useEffect(() => {if (!lightbox && !mapView) return; const close = (event: KeyboardEvent) => {if (event.key === "Escape") {setLightbox(null); setMapView(null);} }; window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close);}, [lightbox, mapView]);
  const allObjectiveItems = (task.objectives || []).flatMap((objective: any) => (objective.items || []).map((item: any) => ({...item, objective}))), bringItems = allObjectiveItems.filter(({objective}: any) => ["plantItem", "mark", "useItem", "buildItem"].includes(objective.type)), turnInItems = allObjectiveItems.filter(({objective}: any) => ["giveItem", "findItem"].includes(objective.type)), groupItems = (entries: any[]) => {const grouped = new Map<string, {item: any; objectives: any[];}>(); for (const entry of entries) {const key = String(entry.id || entry.name || entry.shortName), current = grouped.get(key) || {item: entry, objectives: []}; if (!current.objectives.some(objective => objective.id === entry.objective.id)) current.objectives.push(entry.objective); grouped.set(key, current);} return [...grouped.values()];}, bringGroups = groupItems(bringItems), turnInGroups = groupItems(turnInItems), showPreparation = requiredKeys.length > 0 || bringGroups.length > 0 || turnInGroups.length > 0, conditionObjectives = (task.objectives || []).filter((objective: any) => ["shoot", "extract", "buildItem", "useItem"].includes(objective.type)), visualObjectiveTypes = new Set(["visit", "mark", "plantItem", "extract", "findQuestItem", "questItem"]), needsLocationImages = (task.objectives || []).some((objective: any) => visualObjectiveTypes.has(objective.type) || objective.zones?.some((zone: any) => zone.position)), taskMaps = taskMapReferences(task).map((map: any) => {const objectives = (task.objectives || []).filter((objective: any) => (objective.maps || []).some((candidate: any) => sameMapLocation(candidate, map))), objective = objectives.find((entry: any) => entry.zones?.some((zone: any) => zone.position)) || objectives[0] || task.objectives?.[0], mapIds = map.sourceIds || [map.id], zone = objective?.zones?.find((entry: any) => mapIds.includes(entry.map) && entry.position) || objective?.zones?.find((entry: any) => entry.position); return {map, objectives, objective, zone};});
  const openObjectiveMap = (map: any, zone: any, objective: any, description: string) => setMapView({map, zone, description, objectiveId: objective?.id || "", taskId: task.id, taskName: task.name}), openTaskMap = ({map, objective, zone}: any) => openObjectiveMap(map, zone, objective, objective ? jp(objective.descriptionJa, objective.description) : task.name);
  return <article className={`taskDetail ${needsLocationImages ? "hasLocationObjectives" : "noLocationObjectives"}`}>
    {conditionObjectives.some((objective: any) => objective.type === "shoot") && <section className="killObjectiveSummary"><header><small>ELIMINATION TARGET</small><h3>討伐目標</h3></header><div>{conditionObjectives.filter((objective: any) => objective.type === "shoot").map((objective: any) => <article key={objective.id}><span>倒す人数</span><strong>{Number(objective.count || 1).toLocaleString()}</strong><b>体</b><p>{jp(objective.descriptionJa, objective.description)}</p></article>)}</div></section>}
    {(task.objectives || []).some((objective: any) => objective.type === "buildWeapon" || objective.type === "buildItem") && <WeaponBuildGuide builds={weaponBuilds} loading={weaponBuildLoading} verified={weaponBuildVerified} wiki={task.wiki} />}
    <div className="taskHero">{task.image && <img src={task.image} alt="" />}<div><small>{task.trader} · LV.{task.level}{translating ? " · 説明を日本語へ翻訳中…" : ""}</small><h2>{task.name}</h2>{task.liveVerification && <div className="taskMeta"><span className="liveVerified">✓ PATCH 1.1 ゲーム内確認済み</span></div>}</div></div>
    {task.prerequisites?.length > 0 && <section className="prerequisites"><h3>解放条件</h3>{task.prerequisites.map((x: any) => <span key={x.id}>{x.name}</span>)}</section>}
    {showPreparation && <section className="taskPreparation"><div className="detailTitle"><small>BEFORE YOU RAID</small><h3>必要な準備・納品</h3><p>このタスクで実際に必要なものだけを表示しています。</p></div><div className="preparationGrid">{requiredKeys.length > 0 && <article className="prepKeys"><header><span>01</span><div><small>ACCESS</small><h4>必要な鍵</h4></div></header><div className="prepItems">{requiredKeys.map(key => <div key={`${key.objectiveId || "task"}-${key.id}`}><img src={key.image || `https://assets.tarkov.dev/${key.id}-grid-image.webp`} alt="" /><span><b>{key.nameJa || key.shortName || key.name}</b>{key.nameJa && <small>{key.name}</small>}</span><em>必要</em></div>)}</div></article>}{bringGroups.length > 0 && <article className="prepBring"><header><span>02</span><div><small>TAKE INTO RAID</small><h4>持っていく物</h4></div></header><div className="prepItems">{bringGroups.map(({item, objectives}: any) => <div key={item.id || item.name}><img src={item.image || `https://assets.tarkov.dev/${item.id}-grid-image.webp`} alt="" /><span><b>{item.nameJa || item.shortName || item.name || "指定アイテム"}</b>{objectives.map((objective: any) => <small key={objective.id}>・{objective.count || 1}個持っていく — {jp(objective.descriptionJa, objective.description)}</small>)}</span><em>持込</em></div>)}</div></article>}{turnInGroups.length > 0 && <article className="prepTurnIn"><header><span>03</span><div><small>OBTAIN / HAND OVER</small><h4>入手・納品する物</h4></div></header><div className="prepItems">{turnInGroups.map(({item, objectives}: any) => {const found = objectives.filter((objective: any) => objective.type === "findItem"), handOver = objectives.filter((objective: any) => objective.type === "giveItem"), displayCount = Math.max(1, ...objectives.map((objective: any) => Number(objective.count) || 1)); return <div key={item.id || item.name}><img src={item.image || `https://assets.tarkov.dev/${item.id}-grid-image.webp`} alt="" /><span><b>{item.nameJa || item.shortName || item.name || "指定アイテム"} ×{displayCount}</b>{found.map((objective: any) => <small key={objective.id}>・レイドで{objective.count || 1}個見つける{objective.foundInRaid ? "（FIR）" : ""}</small>)}{handOver.map((objective: any) => <small key={objective.id}>・{objective.count || 1}個納品する{objective.foundInRaid ? "（FIR）" : ""}</small>)}</span><em className={objectives.some((objective: any) => objective.foundInRaid) ? "firRequired" : ""}>{objectives.some((objective: any) => objective.foundInRaid) ? "FIR" : "納品"}</em></div>;})}</div></article>}</div></section>}
    {(taskMaps.length > 0 || task.wiki) && <section className="taskMaps"><div className="detailTitle"><small>TASK MAPS</small><h3>関連マップ・攻略</h3><p>タスク全体に関係するマップと攻略画像を、手順に入る前にまとめています。</p></div>{taskMaps.length > 0 && <div className="taskMapList">{taskMaps.map(({map, objectives, objective, zone}: any) => <button type="button" className="taskMapButton" key={map.id || map.name} onClick={() => openTaskMap({map, objective, zone})}><span>⌖ MAP</span><strong>{map.nameJa || map.name}</strong><small>{objectives.length ? `対象手順 ${objectives.map((entry: any) => String((task.objectives || []).indexOf(entry) + 1).padStart(2, "0")).join("・")}` : "このタスクの対象マップ"}</small></button>)}</div>}{task.wiki && <a className="taskSpecificMap" href={task.wiki} target="_blank" rel="noreferrer">このタスク専用の攻略ページ・画像を確認 ↗</a>}</section>}
    <section className="objectiveSection"><div className="detailTitle"><small>STEP BY STEP</small><h3>達成手順・目的地点</h3></div>{task.objectives.map((objective: any, index: number) => {const description = jp(objective.descriptionJa, objective.description); return <div className="objectiveCard" key={objective.id}><b>{String(index + 1).padStart(2, "0")}</b><div><small>{objectiveLabel[objective.type] || objective.type}{objective.count > 1 ? ` ×${objective.count}` : ""}{objective.foundInRaid ? " · FIR" : ""}</small><h4>{description}</h4>{description !== objective.description && <p>{objective.description}</p>}{objective.zones?.some((zone: any) => zone.position) && <div className="zoneCoordinates">{objective.zones.filter((zone: any) => zone.position).slice(0, 3).map((zone: any) => <span key={`${zone.id}-${zone.map}`}>目的地点座標　X {zone.position.x.toFixed(1)} / Z {zone.position.z.toFixed(1)}</span>)}</div>}{objective.items?.length > 0 && <div className="guideItems">{objective.items.map((item: any) => <div key={item.id}><img src={item.image || `https://assets.tarkov.dev/${item.id}-grid-image.webp`} onError={e => {const img = e.currentTarget; if (!img.dataset.fallback) {img.dataset.fallback = "1"; img.src = asset(`items/${item.id}.webp`);} }} alt={item.name || item.shortName || "アイテム"} /><span><strong>{item.nameJa || item.shortName || item.name}</strong><small>{item.name}</small></span></div>)}</div>}</div></div>;})}</section>
    {media.length > 0 && <section className="locationMedia"><div className="detailTitle"><small>VISUAL LANDMARKS</small><h3>建物・室内・目印の参考画像</h3><p>画像をクリックすると全体を拡大できます。画像は公開Wikiのタスクページから取得しています。</p></div><div>{media.map(image => <figure key={image.url} role="button" tabIndex={0} onClick={() => setLightbox(image)} onKeyDown={e => {if (e.key === "Enter" || e.key === " ") setLightbox(image);}}><img src={image.url} alt={image.caption} loading="lazy" /><figcaption>{image.caption}<span>クリックして拡大 ⛶</span></figcaption></figure>)}</div></section>}
    {lightbox && <div className="imageLightbox" role="dialog" aria-modal="true" aria-label="参考画像の拡大表示" onClick={() => setLightbox(null)}><button type="button" onClick={() => setLightbox(null)} aria-label="拡大表示を閉じる">×</button><div onClick={e => e.stopPropagation()}><img src={lightbox.url} alt={lightbox.caption} /><p>{lightbox.caption}</p></div><small>背景をクリック、または Esc キーで閉じます</small></div>}
    {mapView && <TaskMapModal view={mapView} media={media} mediaLoading={false} wiki={task.wiki} onClose={() => setMapView(null)} />}
  </article>;
}
function WeaponBuildGuide({builds, loading, verified, wiki}: {builds: any[]; loading: boolean; verified: boolean; wiki: string;}) {
  const labels: Record<string, string> = {Ergonomics: "エルゴノミクス", Recoil: "反動", VerticalRecoil: "垂直反動", HorizontalRecoil: "水平反動", Weight: "重量", Durability: "耐久値", MagazineCapacity: "マガジン装弾数", SightingRange: "照準距離", MuzzleVelocity: "初速", Accuracy: "精度"}, comparison = (method: string, value: number) => {const operators: Record<string, string> = {">=": "以上", "=>": "以上", "more": "以上", "<": "未満", "<=": "以下", "=<": "以下", "less": "以下", "=": "ちょうど"}; return `${Number(value).toLocaleString()} ${operators[method] || method || ""}`.trim();};
  if (loading) return <section className="weaponBuildGuide"><div className="weaponBuildLoading"><i></i><b>最新の改造条件を取得中…</b><span>ベース武器・必須パーツ・性能条件を確認しています</span></div></section>;
  if (!builds.length) return <section className="weaponBuildGuide"><header><small>WEAPON BUILD</small><h3>武器改造ガイド</h3></header><div className="weaponBuildUnavailable"><b>{verified ? "このタスクには取得できる改造条件がありません" : "改造条件を取得できませんでした"}</b><p>ネット接続後にページを開き直すか、下の攻略ページで現在の条件を確認してください。</p>{wiki && <a href={wiki} target="_blank" rel="noreferrer">最新の改造条件を確認 ↗</a>}</div></section>;
  return <section className="weaponBuildGuide"><header><small>WEAPON BUILD</small><h3>具体的な改造内容</h3><p>上から順に用意し、最後に性能値を確認してください。</p></header>{builds.map((build, index) => <article className="weaponBuildCard" key={build.id || index}><div className="weaponBase"><span>STEP 1　ベース武器</span>{build.item && <><img src={build.item.inspectImageLink || build.item.gridImageLink || `https://assets.tarkov.dev/${build.item.id}-grid-image.webp`} alt="" /><div><strong>{build.item.name || build.item.shortName}</strong><small>{build.item.shortName}</small></div></>}</div><div className="weaponParts"><h4>STEP 2　取り付ける必須パーツ</h4>{build.parts?.length ? <div>{build.parts.map((part: any) => <article key={part.id}><img src={part.gridImageLink || `https://assets.tarkov.dev/${part.id}-grid-image.webp`} alt="" /><span><b>{part.name || part.shortName}</b><small>{part.shortName}</small></span></article>)}</div> : <p>個別指定の必須パーツはありません。</p>}{build.categories?.length > 0 && <div className="weaponCategories"><b>種類指定</b>{build.categories.map((category: any) => <span key={category.id}>{category.name}</span>)}</div>}</div><div className="weaponStats"><h4>STEP 3　完成後の性能条件</h4>{build.attributes?.length ? <div>{build.attributes.map((attribute: any) => <span key={attribute.name}><small>{labels[attribute.name] || attribute.name}</small><strong>{comparison(attribute.requirement?.compareMethod, attribute.requirement?.value)}</strong></span>)}</div> : <p>数値条件はありません。必須パーツが装着されていることを確認してください。</p>}</div><footer><b>STEP 4</b><span>武器の耐久・折り畳み状態・弾薬やマガジンを含めて条件を再確認し、Mechanicへ納品します。</span></footer></article>)}</section>;
}
function TaskMapModal({view, media, mediaLoading, wiki, onClose}: {view: any, media: TaskMedia[], mediaLoading: boolean, wiki: string, onClose: () => void;}) {
  const sanitary = view.taskId === "59689ee586f7740d1570bbd5" && ["6a576ccec23300f0309ab511", "6a56800fc1311a6bb4cecf9b"].includes(view.objectiveId), normalize = (value: string) => String(value || "").toLowerCase().replace(/[^a-z0-9ぁ-んァ-ヶ一-龠]/g, ""), mapName = normalize(view.map.name || view.map.nameJa || view.map.slug || ""), words = String(view.description || "").toLowerCase().replace(/[^a-z0-9ぁ-んァ-ヶ一-龠 ]/g, " ").split(/\s+/).filter((word: string) => word.length >= 3), scored = media.map((item, index) => ({item, index, score: (normalize(item.caption).includes(mapName) ? 4 : 0) + (/map|地図|location|場所/i.test(item.caption) ? 3 : 0) + words.reduce((score: number, word: string) => score + (normalize(item.caption).includes(normalize(word)) ? 1 : 0), 0)})).sort((a, b) => b.score - a.score), sanitarySpecific = sanitary ? (media.find(item => /sanitary|gas.?analy|laboratory|med.?lab|window|room|stash/i.test(item.caption) && !/banner|icon/i.test(item.caption)) || media.find(item => !/map|地図|banner|icon|logo/i.test(item.caption))) : undefined, specific = sanitarySpecific || (!sanitary ? scored.find(candidate => candidate.score >= 4)?.item : undefined), mapUrl = mapImageFor(view.map), image = specific ? {...specific, specific: true} : {url: sanitary ? "" : mapUrl, caption: sanitary ? "" : `${view.map.name || view.map.nameJa} 全体マップ`, specific: false}, landmarks = (sanitary ? media : scored.filter(candidate => candidate.item.url !== image.url && candidate.score > 0).map(candidate => candidate.item)).filter(item => item.url !== image.url).slice(0, 6), bounds = mapBounds[view.map.id], position = view.zone?.position; let marker: null | {left: number; top: number;} = null;
  if (!image.specific && bounds && position) {const [[startX, startZ], [endX, endZ]] = bounds, left = (position.x - startX) / (endX - startX) * 100, top = (position.z - startZ) / (endZ - startZ) * 100; marker = {left: Math.max(1, Math.min(99, left)), top: Math.max(1, Math.min(99, top))};}
  return <div className="taskMapModal" role="dialog" aria-modal="true" aria-label={`${view.map.name}の目的地マップ`} onClick={onClose}><button className="taskMapClose" type="button" onClick={onClose}>×</button><section onClick={e => e.stopPropagation()}><header><div><small>QUEST LOCATION MAP</small><h2>{view.map.nameJa || view.map.name}</h2><p>{view.description}</p></div>{position && <div className="mapCoordinate"><span>目的地座標</span><b>X {position.x.toFixed(1)} / Z {position.z.toFixed(1)}</b></div>}</header><div className="taskMapBody">{sanitary && <section className="sanitaryRoute"><header><small>目的地を確認済み</small><h3>新研究室 2階・窓際</h3><p>医療テントではありません。オフィスからスカイブリッジを渡った反対側の新しい研究室へ向かいます。</p></header><ol><li><b>1</b><span>Factoryのオフィス棟へ向かう</span></li><li><b>2</b><span>オフィス側からスカイブリッジを渡る</span></li><li><b>3</b><span>橋の突き当たりにある研究室へ入る</span></li><li><b>4</b><span>2階の部屋にある窓際でガスアナライザーを設置</span></li></ol><strong>注意：夜Factoryでは設置判定が出ない報告があります。昼Factoryで実行してください。</strong></section>}<div className="taskMapGuideSteps"><span><b>01</b>全体マップで位置を確認</span><span><b>02</b>建物・入口の外観を確認</span><span><b>03</b>室内・対象物を確認</span></div>{mediaLoading ? <div className="taskMapLoading"><i></i><b>目的地画像を読み込み中…</b><span>タスク専用マップと建物・入口画像を確認しています</span></div> : <>{image.url ? <><div className="taskMapCanvas"><img src={image.url} alt={`${view.map.name}のタスク目的地マップ`} />{image.specific && <strong className="taskMapVerified">TASK-SPECIFIC MAP</strong>}{marker && <div className="questMapMarker" style={{left: `${marker.left}%`, top: `${marker.top}%`}}><i></i><b>目的地</b></div>}</div><p className="taskMapCaption">{image.caption}</p></> : <div className="taskMapUnavailable"><b>目的地画像を取得できませんでした</b><p>右下のタスク専用攻略ページから確認できます。</p></div>}{landmarks.length > 0 && <section className="taskLandmarks"><header><div><small>VISUAL ROUTE</small><h3>{sanitary ? "研究室・窓際のタスク専用画像" : "建物・入口・室内の参考画像"}</h3></div><span>{landmarks.length}枚</span></header><div>{landmarks.map((item, index) => <a href={item.url} target="_blank" rel="noreferrer" key={item.url}><i>{String(index + 1).padStart(2, "0")}</i><img src={item.url} alt={item.caption} /><span>{item.caption}<small>クリックして原寸表示</small></span></a>)}</div></section>}</>}</div><footer>{mediaLoading ? <span>目的地画像を準備しています</span> : image.specific ? <span className="markerReady">● このタスク専用の目的地画像です</span> : marker ? <span className="markerReady">● 赤い印が目的地です</span> : sanitary ? <span className="markerReady">● 上の手順で新研究室2階へ向かってください</span> : <span>全体マップと下の参考画像を照合してください</span>}{wiki && <a href={wiki} target="_blank" rel="noreferrer">タスク専用攻略ページを確認 ↗</a>}</footer></section></div>;
}
