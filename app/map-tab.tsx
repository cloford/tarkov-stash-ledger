"use client";
import {useEffect, useLayoutEffect, useMemo, useRef, useState} from "react";
import bundledMapData from "./data/maps-cache-v3.json";
import {mapLocation, sameMapLocation} from "./map-normalization.mjs";
import {mapImageCacheChecks, mapVariantRequests} from "./map-request-cache.mjs";
import {normalizeMapRuntime} from "./runtime-data.mjs";
import type {KeyMapFocus, MapDataResult, MapVariant} from "./app-types";

const asset = (path: string) => typeof window !== "undefined" && window.location.protocol === "file:" ? `./${path}` : `/${path}`;

type MapVisual = {image: string; source: "annotated" | "raster"; size?: readonly [number, number]; revision: string;};
const mapVisuals: Record<string, MapVisual> = {
  "55f2d3fd4bdc2d5f408b4567": {image: "https://www.re3mr.com/maps/Factory/FactorybyRe3mr.png", source: "annotated", size: [13440, 6656], revision: "factory-re3mr-13440x6656-v1"},
  "56f40101d2720b2a4d8b45d6": {image: "https://maps.reemr.se/Customs/re3mrCustoms2.png", source: "annotated", size: [7832, 5016], revision: "customs-re3mr-7832x5016-v2"},
  "5704e3c2d2720bac5b8b4567": {image: "https://www.reemr.se/maps/Woods/WoodsRe3mrPNG.png", source: "annotated", size: [7680, 5168], revision: "woods-re3mr-7680x5168-v1"},
  "5704e554d2720bac5b8b456e": {image: "https://reemr.se/maps/Shoreline/re3mrShoreline2.png", source: "annotated", size: [5760, 3240], revision: "shoreline-re3mr-5760x3240-v2"},
  "5714dbc024597771384a510d": {image: "https://maps.reemr.se/Interchange/re3mrInterchange.png", source: "annotated", size: [9600, 5400], revision: "interchange-re3mr-9600x5400-v1"},
  "5b0fc42d86f7744a585f9105": {image: "https://tarkov.dev/maps/labs-2d.jpg", source: "raster", size: [3820, 1980], revision: "labs-tarkovdev-3820x1980-v1"},
  "5704e5fad2720bc05b8b4567": {image: "https://reemr.se/maps/Reserve/Re3mrReserveLossless.png", source: "annotated", size: [5760, 3240], revision: "reserve-re3mr-5760x3240-v1"},
  "5704e4dad2720bb55b8b4567": {image: "https://reemr.se/maps/Lighthouse/re3mrLighthouseVERT.png", source: "annotated", size: [8259, 7560], revision: "lighthouse-re3mr-8259x7560-v1"},
  "5714dc692459777137212e12": {image: "https://reemr.se/maps/Streets/re3mrStreetsofTarkov.png", source: "annotated", size: [7605, 4841], revision: "streets-re3mr-7605x4841-v1"},
  "653e6760052c01c1c805532f": {image: "https://www.re3mr.com/maps/Groundzero/GroundZero.png", source: "annotated", size: [2656, 2160], revision: "groundzero-re3mr-2656x2160-v1"},
  "65b8d6f5cdde2479cb2a3125": {image: "https://www.re3mr.com/maps/Groundzero/GroundZero.png", source: "annotated", size: [2656, 2160], revision: "groundzero-re3mr-2656x2160-v1"}
};
const mapBounds: Record<string, [[number, number], [number, number]]> = {
  "55f2d3fd4bdc2d5f408b4567": [[77, -64.5], [-65.5, 67.4]],
  "56f40101d2720b2a4d8b45d6": [[698, -307], [-372, 237]],
  "5704e3c2d2720bac5b8b4567": [[646, -914], [-761, 442]],
  "5704e554d2720bac5b8b456e": [[504, -415], [-1056, 618]],
  "5714dbc024597771384a510d": [[598, -442], [-433, 426]],
  "5b0fc42d86f7744a585f9105": [[-80, -477], [-287, -193]],
  "5704e5fad2720bc05b8b4567": [[289, -293], [-303, 244]],
  "5704e4dad2720bb55b8b4567": [[515, -998], [-545, 725]],
  "5714dc692459777137212e12": [[323, -295], [-280, 532]],
  "653e6760052c01c1c805532f": [[249, -124], [-99, 364]],
  "65b8d6f5cdde2479cb2a3125": [[249, -124], [-99, 364]]
};
const namedMapVisuals: Record<string, MapVisual> = {"the labyrinth": {image: "https://tarkov.dev/maps/labyrinth-2d.jpg", source: "raster", size: [4800, 4320], revision: "labyrinth-tarkovdev-4800x4320-v1"}};
const fallbackMapImages: Record<string, string> = {"55f2d3fd4bdc2d5f408b4567": "https://tarkov.dev/maps/factory-2d.jpg", "59fc81d786f774390775787e": "https://tarkov.dev/maps/factory-2d.jpg", "56f40101d2720b2a4d8b45d6": "https://tarkov.dev/maps/customs-2d.jpg", "5704e3c2d2720bac5b8b4567": "https://tarkov.dev/maps/woods-2d.jpg", "5704e554d2720bac5b8b456e": "https://www.escapistmagazine.com/wp-content/uploads/2023/07/Escape-from-Tarkov-Shoreline-Map.jpg?fit=1200%2C800", "5714dbc024597771384a510d": "https://tarkov.dev/maps/interchange-2d.jpg", "5704e4dad2720bb55b8b4567": "https://tarkov.dev/maps/lighthouse-2d.jpg", "5704e5fad2720bc05b8b4567": "https://tarkov.dev/maps/reserve-2d.jpg", "5714dc692459777137212e12": "https://tarkov.dev/maps/streets-2d.jpg", "653e6760052c01c1c805532f": "https://tarkov.dev/maps/ground-zero-2d.jpg", "65b8d6f5cdde2479cb2a3125": "https://tarkov.dev/maps/ground-zero-2d.jpg", "68236e8153654e8c1200798a": "https://tarkov.dev/maps/ground-zero-2d.jpg", "5b0fc42d86f7744a585f9105": "https://tarkov.dev/maps/labs-2d.jpg", "6a294a5b5eb5f9a1700417b7": "https://tarkov.dev/maps/labs-2d.jpg"};
const fallbackMapImagesByName: Record<string, string> = {factory: "https://tarkov.dev/maps/factory-2d.jpg", "night factory": "https://tarkov.dev/maps/factory-2d.jpg", customs: "https://tarkov.dev/maps/customs-2d.jpg", woods: "https://tarkov.dev/maps/woods-2d.jpg", shoreline: "https://tarkov.dev/maps/shoreline-2d.jpg", interchange: "https://tarkov.dev/maps/interchange-2d.jpg", lighthouse: "https://tarkov.dev/maps/lighthouse-2d.jpg", reserve: "https://tarkov.dev/maps/reserve-2d.jpg", "streets of tarkov": "https://tarkov.dev/maps/streets-2d.jpg", streets: "https://tarkov.dev/maps/streets-2d.jpg", "ground zero": "https://tarkov.dev/maps/ground-zero-2d.jpg", "ground zero 21+": "https://tarkov.dev/maps/ground-zero-2d.jpg", "ground zero tutorial": "https://tarkov.dev/maps/ground-zero-2d.jpg", "the lab": "https://tarkov.dev/maps/labs-2d.jpg", "the lab (dark)": "https://tarkov.dev/maps/labs-2d.jpg", labs: "https://tarkov.dev/maps/labs-2d.jpg", icebreaker: "https://tarkov.dev/maps/icebreaker-2d.jpg", terminal: "https://tarkov.dev/maps/terminal-2d.jpg", "the labyrinth": "https://tarkov.dev/maps/labyrinth-2d.jpg"};
const mapVisualFor = (map: any) => mapVisuals[String(map.id || "")] || namedMapVisuals[String(map.name || map.nameJa || "").trim().toLowerCase()];
const mapImageFor = (map: any) => {const id = String(map.id || ""), name = String(map.name || map.nameJa || "").trim().toLowerCase(), slug = String(map.slug || name).trim().toLowerCase().replace(/\+/g, "").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); return mapVisualFor(map)?.image || fallbackMapImages[id] || fallbackMapImagesByName[name] || fallbackMapImagesByName[String(map.slug || "").toLowerCase()] || `https://tarkov.dev/maps/${slug}-2d.jpg`;};
const mapStages = [
  {name: "Ground Zero", ja: "グラウンドゼロ", id: "653e6760052c01c1c805532f", desc: "市街地中心部。建物内と地下通路が多く、初心者は現在地の確認が重要です。", exits: ["Emercom Checkpoint", "Mira Ave", "Nakatani Basement Stairs"]},
  {name: "Streets of Tarkov", ja: "ストリート・オブ・タルコフ", id: "5714dc692459777137212e12", desc: "広大な市街地。通り名と大きな建物を目印に移動します。", exits: ["Damaged House", "Evacuation Zone", "Klimov Street"]},
  {name: "Customs", ja: "カスタム", id: "56f40101d2720b2a4d8b45d6", desc: "道路と川で東西に分かれた定番マップ。赤倉庫・寮・新建築を覚えると迷いにくくなります。", exits: ["Crossroads", "ZB-1011", "RUAF Roadblock"]},
  {name: "Woods", ja: "ウッズ", id: "5704e3c2d2720bac5b8b4567", desc: "森林中心のマップ。山・湖・製材所・送電線で方向を確認します。", exits: ["Outskirts", "UN Roadblock", "ZB-014"]},
  {name: "Interchange", ja: "インターチェンジ", id: "5714dbc024597771384a510d", desc: "巨大商業施設ULTRAが中心。店舗名と屋外の方角をセットで覚えます。", exits: ["Emercom Checkpoint", "Railway Exfil", "Hole in the Fence"]},
  {name: "Factory", ja: "ファクトリー", id: "55f2d3fd4bdc2d5f408b4567", desc: "小規模な屋内戦マップ。扉番号・地下・オフィス階段が主な目印です。", exits: ["Gate 3", "Cellars", "Med Tent Gates"]},
  {name: "Reserve", ja: "リザーブ", id: "5704e5fad2720bc05b8b4567", desc: "軍事基地。建物のチェス記号と地下通路を覚えると移動しやすくなります。", exits: ["D-2", "Cliff Descent", "Armored Train"]},
  {name: "Lighthouse", ja: "ライトハウス", id: "5704e4dad2720bb55b8b4567", desc: "海岸沿いに細長いマップ。幹線道路・山側・浄水場の位置関係が重要です。", exits: ["Path to Shoreline", "Northern Checkpoint", "Southern Road"]},
  {name: "Shoreline", ja: "ショアライン", id: "5704e554d2720bac5b8b456e", desc: "保養所を中心とした広い自然マップ。海岸・川・発電所で方角を確認します。", exits: ["Path to Lighthouse", "Road to Customs", "Tunnel"]},
  {name: "The Lab", ja: "研究所", id: "5b0fc42d86f7744a585f9105", desc: "高難度の屋内施設。各区画の色とエレベーター、電源操作を確認します。", exits: ["Cargo Elevator", "Medical Block Elevator", "Parking Gate"]},
  {name: "Terminal", ja: "ターミナル", id: "", desc: "港湾ターミナル。遮蔽物と建物番号を確認して移動します。", exits: []},
  {name: "Icebreaker", ja: "アイスブレーカー", id: "", desc: "船内を進む特殊マップ。甲板・機関室・通路階層を確認します。", exits: []},
  {name: "The Labyrinth", ja: "ラビリンス", id: "", desc: "複雑な閉所マップ。入口、分岐、階層を一つずつ確認します。", exits: []}
];
function mapStageFor(reference: any) {return mapStages.find(stage => sameMapLocation(stage, typeof reference === "string" ? {name: reference} : reference));}
const stageHotspots = [
  {name: "Streets of Tarkov", left: 40.2, top: 15.6, width: 18, height: 6}, {name: "The Lab", left: 28.8, top: 18.7, width: 9, height: 6},
  {name: "Interchange", left: 41.8, top: 32.1, width: 14, height: 6}, {name: "Woods", left: 25.9, top: 37, width: 11, height: 6},
  {name: "Customs", left: 38.6, top: 39.6, width: 12, height: 6}, {name: "Factory", left: 30.2, top: 49.2, width: 10, height: 6},
  {name: "Reserve", left: 19.8, top: 57.8, width: 10, height: 6}, {name: "Lighthouse", left: 18.6, top: 70.8, width: 13, height: 6},
  {name: "Shoreline", left: 25.2, top: 81, width: 12, height: 6}, {name: "Terminal", left: 35.4, top: 78.2, width: 12, height: 6}
];
const factoryExtracts = [
  {name: "Courtyard Gate", faction: "PMC", direction: "西（W）", requirement: "鍵不要・常時利用可能", description: "屋外のトラック前にある中庭ゲート。トランジット判定より奥まで進むと脱出判定に入ります。", image: "https://tarkov101.com/wp-content/uploads/2024/10/FactoryToWoods.png"},
  {name: "Cellars", faction: "PMC", direction: "北東（NE）・地下", requirement: "Factory emergency exit keyが必要", description: "区画2側から地下道へ降り、階段裏の扉を抜けた先。誰かが開錠済みなら鍵なしでも通れます。", image: "https://primagames.com/wp-content/uploads/2024/08/Factory-Gate-2-Going-to-cellars-EFT.jpg?resize=1024"},
  {name: "Gate 0", faction: "PMC", direction: "南西（SW）", requirement: "鍵不要・常時利用可能", description: "地上階の大きな「0」表示が目印。Ver.0.15以降は鍵なしで利用できます。", image: "https://www.escapistmagazine.com/wp-content/uploads/2023/06/Escape-From-Tarkov-Scout-Gate-0-Extract.png"},
  {name: "Med Tent Gate", faction: "PMC", direction: "南東（SE）", requirement: "Factory emergency exit keyが必要", description: "医療テントの奥にある赤いゲート。テントと壁の隙間からゲート内へ入れます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/FACTORY-ESC-MED_TENT_GATES-others_3.jpg.webp?rev=68581170cc1a7b561fed7119135926f7&t=20231203163300"},
  {name: "Camera Bunker Door", faction: "Scav", direction: "南西（SW）・地下", requirement: "SCAV専用・条件なし", description: "地下バンカーの監視カメラ付近にある扉。暗い地下道と積まれた樽が目印です。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aattach/FACTORY-ESC-CAMERA_BUNKER_DOOR-Rt_Door.jpg?rev=effa8d499f05df11981ff1748a97e0ce&t=20231013142425"},
  {name: "Office Window", faction: "Scav", direction: "西（W）・3階", requirement: "SCAV専用・条件なし", description: "オフィス3階の窓際。机、PC、壊れたブラインドがある部屋の窓側に立ちます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aattach/FACTORY-ESC-OFFICE_WINDOW-Rt_Esc.jpg?rev=134005d2d934e07ea71875e1a5fe3dba&t=20231013143355"},
  {name: "Gate 3", faction: "All", direction: "北西（NW）", requirement: "PMC・SCAV共通・条件なし", description: "大きな数字の3がある赤いゲート。最初のゲートから青い箱の区画を通り、さらに奥の扉へ進みます。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/Gate-3_1_ver0.15.png.webp?rev=4f7cf99cc5f5b1e81654a23eefea7cee&t=20241226192904"},
  {name: "Smugglers' Passage", faction: "All", direction: "東（E）・地下", requirement: "Note with code word Arkが必要", description: "Cellarsへ向かう扉と逆方向の地下通路。レイド内でArkのメモを見つけ、出口前で引き渡します。", image: "https://cdn.wikiwiki.jp/to/w/eft/img/%3A%3Aref/FactoryMap.jpg?rev=67da37a9b06e89e3b94bfbe58b2c57b6&t=20180818200634"}
];
const labExtracts = [
  {name: "Medical Block Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Cargo Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Main Elevator", faction: "All", requirement: "電源を復旧してエレベーターを使用"},
  {name: "Ventilation Shaft", faction: "PMC", requirement: "バックパック装備不可"},
  {name: "Sewage Conduit", faction: "PMC", requirement: "排水操作が必要"},
  {name: "Parking Gate", faction: "PMC", requirement: "ゲート開放操作が必要"},
  {name: "Hangar Gate", faction: "PMC", requirement: "ゲート開放操作が必要"}
];
const labyrinthExtracts = [
  {name: "The Way Up", faction: "PMC"},
  {name: "Ariadne's Path", faction: "PMC"}
];
type PercentAnnotationPart = {left: number; top: number; width: number; height: number;};
type PercentAnnotationAnchor = {left: number; top: number; width?: number; height?: number; focusLeft?: number; focusTop?: number; parts?: PercentAnnotationPart[];};
type PixelRect = {x: number; y: number; width: number; height: number;};
type MapAnnotation = {focusPoint: {x: number; y: number;}; labelBounds: PixelRect[]; hitBounds: PixelRect[]; lines: number; revision: string;};
const printedLabelKey = (value: any) => String(value || "").normalize("NFKC").toLowerCase().replace(/[・･]/g, "").replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]/g, "");
const printedLabelAnchors: Record<string, Record<string, PercentAnnotationAnchor>> = {
  "5714dc692459777137212e12": {
    expocheckpoint: {left: 54.4, top: 17.64}, cardinalapartmentcomplexparking: {left: 61.02, top: 16.74}, stylobatebuildingelevator: {left: 76.8, top: 26.64}, klimovshoppingmallexfil: {left: 87.29, top: 35.86}, klimovstreetflare: {left: 91.96, top: 49.28}, pinewoodbasementcoop: {left: 81.89, top: 43.19}, pinewoodbasement: {left: 81.89, top: 43.19}, sewerriver: {left: 91.29, top: 61.65}, entrancetocatacombs: {left: 91.68, top: 70.3}, damagedhouse: {left: 87.65, top: 76.99}, ventilationshaft: {left: 77.44, top: 83.83}, courtyard: {left: 77.13, top: 98.32}, primorskyavetaxivex: {left: 62.78, top: 93.98}, crashsite: {left: 37.51, top: 81.14}, sewermanhole: {left: 42.17, top: 67.18}, collapsedcrane: {left: 46.79, top: 60.3}, nearkamchatskayaarch: {left: 47.11, top: 33.7, width: 6.92, height: 3.45}, smugglersbasement: {left: 64.3, top: 40.89}, グラウンドゼロへ移動: {left: 93.43, top: 53.21}, インターチェンジへ移動: {left: 35.39, top: 87.24}, 研究所へ移動: {left: 50.27, top: 41.47}
  },
  "56f40101d2720b2a4d8b45d6": {
    zb013: {left: 51.25, top: 28.4, width: 2.2, height: 0.9}, dormsvex: {left: 51.59, top: 75.73}, zb1011: {left: 6.91, top: 30.68}, crossroads: {left: 97.03, top: 47.03}, oldgasstation: {left: 35.5, top: 27.14}, trailerpark: {left: 94.95, top: 24.01}, ruafroadblock: {left: 66.8, top: 32.75, width: 4.1, height: 2.7, focusLeft: 66.8, focusTop: 33.2}, militarybasecp: {left: 6.39, top: 69.06}, militarycheckpoint: {left: 6.09, top: 66.53}, passagebetweenrocks: {left: 13.32, top: 73.39}, railroadtomilitarybase: {left: 20.24, top: 75.2}, oldroadgate: {left: 44.83, top: 75.33}, sniperroadblock: {left: 61.34, top: 66.51}, railroadtoport: {left: 78.62, top: 60}, trailerparkworkersshack: {left: 89.15, top: 23.67}, railroadtotarkov: {left: 79.98, top: 24.95}, warehouse17: {left: 63.1, top: 35.81}, factoryshacks: {left: 53.18, top: 32.4}, warehouse4: {left: 31.65, top: 34.35, width: 3.15, height: 2.13}, oldgasstationgate: {left: 35.5, top: 27.14, width: 4.5, height: 2.2}, factoryfarcorner: {left: 4.11, top: 32.65}, administrationgate: {left: 2.16, top: 43.68, width: 3.99, height: 1.35}, railroadpassageflare: {left: 48.47, top: 22}, scavcheckpoint: {left: 3.68, top: 51.88, width: 2.97, height: 2.34}, boilerroombasementcoop: {left: 56.2, top: 30.2, width: 5.5, height: 2.8}, smugglersboat: {left: 72.11, top: 67.72}, smugglersbunkerzb1012: {left: 22.43, top: 32.12}, リザーブへ移動: {left: 3.99, top: 67.96}, ファクトリーへ移動: {left: 33.4, top: 27.23}, インターチェンジへ移動: {left: 97.66, top: 25.77}, ショアラインへ移動: {left: 65.12, top: 66.65}
  },
  "5704e3c2d2720bac5b8b4567": {
    friendshipbridgecoop: {left: 44.05, top: 8.53}, ruafgate: {left: 54.24, top: 74.89}, ruafroadblock: {left: 54.24, top: 74.89}, zb016: {left: 73.28, top: 52.96}, zb014: {left: 19.4, top: 56.9, width: 2.15, height: 1}, unroadblock: {left: 85.3, top: 67.7, width: 4.35, height: 1.15}, northernunroadblock: {left: 85.4, top: 50.6, width: 4.5, height: 1.9}, outskirts: {left: 24.1, top: 74.8}, bridgevex: {left: 81.97, top: 27.14}, deadmansplace: {left: 38.15, top: 67.1, width: 5.8, height: 1.35}, boat: {left: 37.33, top: 65.52}, scavhouse: {left: 17.21, top: 65.87}, scavbunker: {left: 51.05, top: 48.05, width: 12, height: 2.6}, easternrocks: {left: 83.8, top: 53.45, width: 2.8, height: 1.9}, mountainstash: {left: 64.2, top: 41.68}, oldrailwaydepot: {left: 79.79, top: 61}, powerlinepassageflare: {left: 14.49, top: 52.41}, railwaybridgetotarkov: {left: 96.73, top: 66.28}, ファクトリーへ移動: {left: 72.5, top: 72.49}, リザーブへ移動: {left: 31.43, top: 73.84}, ライトハウスへ移動: {left: 16.13, top: 72.3}, カスタムへ移動: {left: 56.39, top: 73.22}
  },
  "5704e554d2720bac5b8b456e": {
    pathtolighthouse: {left: 19.06, top: 16.38}, roadtocustoms: {left: 83.23, top: 32.18}, tunnel: {left: 16.12, top: 47.65}, roadtonorthvex: {left: 64.26, top: 3.79}, railwaybridge: {left: 92.73, top: 47.48}, climberstrail: {left: 48.45, top: 5.23}, pierboat: {left: 56.75, top: 73.6, width: 3.7, height: 1.55}, smugglerspathcoop: {left: 73.07, top: 9.95}, adminbasement: {left: 51.07, top: 14.54}, eastwinggymentrance: {left: 56.85, top: 19.62}, lighthouse: {left: 63.46, top: 65.91}, ruinedroad: {left: 17.32, top: 55.85}, mountainbunker: {left: 55.5, top: 7.66}, ライトハウスへ移動: {left: 19.06, top: 16.38}, ターミナルへ移動: {left: 88.14, top: 56.84}, ラビリンスへ移動: {left: 49.18, top: 22.25}
  },
  "55f2d3fd4bdc2d5f408b4567": {
    courtyardgate: {left: 46.87, top: 30.6}, cellars: {left: 92.64, top: 14.48}, gate0: {left: 53.14, top: 65.26}, medtentgate: {left: 71.97, top: 51.3}, camerabunkerdoor: {left: 82.66, top: 47.09}, officewindow: {left: 2.64, top: 34.4}, gate3: {left: 49.84, top: 22.56}, smugglerspassage: {left: 41.23, top: 5.8}
  },
  "653e6760052c01c1c805532f": {
    emercomcheckpoint: {left: 59.86, top: 9.22}, scavcheckpointcoop: {left: 75.84, top: 10.9}, miraaveflare: {left: 50.62, top: 32.13}, policecordonvex: {left: 82.25, top: 42.86, width: 8.5, height: 3.88}, nakatanibasementstairs: {left: 81.3, top: 78.4, width: 13, height: 3, parts: [{left: 75.68, top: 85.32, width: 8.2, height: 1.8}]}, tartowerssalesoffice: {left: 54.7, top: 59.51}, ストリートオブタルコフへ移動: {left: 48.19, top: 42.07}
  },
  "65b8d6f5cdde2479cb2a3125": {
    emercomcheckpoint: {left: 59.86, top: 9.22}, scavcheckpointcoop: {left: 75.84, top: 10.9}, miraaveflare: {left: 50.62, top: 32.13}, policecordonvex: {left: 82.25, top: 42.86, width: 8.5, height: 3.88}, nakatanibasementstairs: {left: 81.3, top: 78.4, width: 13, height: 3, parts: [{left: 75.68, top: 85.32, width: 8.2, height: 1.8}]}, tartowerssalesoffice: {left: 54.7, top: 59.51}, ストリートオブタルコフへ移動: {left: 48.19, top: 42.07}
  },
  "5714dbc024597771384a510d": {
    emercomcheckpoint: {left: 49.48, top: 80.04}, railwayexfil: {left: 5.83, top: 12.51}, powerstationvex: {left: 50.31, top: 25.5}, scavcampcoop: {left: 67.18, top: 47.23, width: 4.38, height: 2.47}, saferoomexfil: {left: 66.58, top: 54.68}, カスタムへ移動: {left: 17.97, top: 96.35}, ストリートオブタルコフへ移動: {left: 17.21, top: 14.9}
  },
  "5704e5fad2720bc05b8b4567": {
    armoredtrain: {left: 26.55, top: 25.48}, d2: {left: 97.52, top: 88.41}, bunkerhermeticdoor: {left: 37.24, top: 18.64, width: 6.31, height: 3.51}, cliffdescent: {left: 58.36, top: 72.56, width: 3.7, height: 1.3}, scavlandscoop: {left: 68.93, top: 6.09, width: 4.47, height: 2.54}, sewermanhole: {left: 43.2, top: 48.88, width: 7.72, height: 2.65}, holeinthewallbythemountains: {left: 91.65, top: 31.28}, heatingpipe: {left: 51.09, top: 2.18}, depothermeticdoor: {left: 26.65, top: 34.74}, checkpointfence: {left: 50.25, top: 69.61, width: 4.99, height: 3.37}, exittowoods: {left: 36.52, top: 5.33}, ライトハウスへ移動: {left: 12.37, top: 26.5}, ウッズへ移動: {left: 12.24, top: 14.75, width: 2.57, height: 1.35}, カスタムへ移動: {left: 74.28, top: 5.24, width: 3.06, height: 2.09}
  },
  "5704e4dad2720bb55b8b4567": {
    roadtomilitarybasevex: {left: 92.36, top: 14.14}, sidetunnelcoop: {left: 72.21, top: 77.05}, mountainpass: {left: 78.34, top: 54.67}, pathtoshoreline: {left: 91.14, top: 50.34}, northerncheckpoint: {left: 57.57, top: 1.72}, southernroad: {left: 84.6, top: 79.6}, southernroadlandslide: {left: 83.98, top: 84.29}, hideoutunderthelandingstage: {left: 51.85, top: 71.3}, scavhideoutatthegrotto: {left: 52.37, top: 29.02}, industrialzonegates: {left: 77.06, top: 11.74}, armoredtrain: {left: 67.07, top: 9.39}, passagebythelake: {left: 91.03, top: 25.06}, ショアラインへ移動: {left: 90.51, top: 50.98}, リザーブへ移動: {left: 83.79, top: 12.91}, ウッズへ移動: {left: 53.72, top: 6.61}
  },
  "5b0fc42d86f7744a585f9105": {
    mainelevator: {left: 17.25, top: 1.95, width: 6.8, height: 1.7}, ventilationshaft: {left: 4.11, top: 40.38, width: 7, height: 2.37}, medicalblockelevator: {left: 16.44, top: 62.17, width: 9, height: 2.2}, sewageconduit: {left: 22.79, top: 51.82, width: 6.8, height: 2.2}, parkinggate: {left: 30.35, top: 65.3, width: 5.4, height: 2.4}, cargoelevator: {left: 72.87, top: 58.33, width: 6.2, height: 2.4}, hangargate: {left: 71.64, top: 77.32, width: 5.2, height: 2.4}
  },
  "name:thelabyrinth": {
    thewayup: {left: 79, top: 43.7, width: 5.2, height: 1.35}, ariadnespath: {left: 44.14, top: 64.27, width: 8.2, height: 1.35}
  }
};
const unprintedLabelKeys: Record<string, Set<string>> = {
  "5714dbc024597771384a510d": new Set(["holeinthefence"]),
  "5b0fc42d86f7744a585f9105": new Set(["ストリートオブタルコフへ移動"])
};
function printedLabelLineLengths(value: any) {
  const label = String(value || "").trim(), compact = label.replace(/\s+/g, "");
  let lineCount = Math.min(3, Math.max(1, Math.ceil(compact.length / 22)));
  if (lineCount === 1) return [label.length];
  const words = label.split(/\s+/).filter(Boolean);
  if (words.length === 1) {const chunk = Math.ceil(label.length / lineCount); return Array.from({length: lineCount}, (_, index) => label.slice(index * chunk, (index + 1) * chunk).length);}
  lineCount = Math.min(lineCount, words.length); let best: number[] | null = null, bestScore = Infinity;
  const search = (start: number, lengths: number[]) => {if (lengths.length === lineCount - 1) {const tail = words.slice(start).join(" ").length, candidate = [...lengths, tail], max = Math.max(...candidate), min = Math.min(...candidate), score = max * 10 + (max - min); if (score < bestScore) {best = candidate; bestScore = score;} return ;} const remainingLines = lineCount - lengths.length - 1; for (let end = start + 1; end <= words.length - remainingLines; end++)search(end, [...lengths, words.slice(start, end).join(" ").length]);};
  search(0, []); return best || [label.length];
}
function printedLabelAnchor(mapId: string, mapName: string, name: any): MapAnnotation | null {
  const points = printedLabelAnchors[mapId] || printedLabelAnchors[`name:${printedLabelKey(mapName)}`], point = points?.[printedLabelKey(name)];
  const visual = mapVisuals[mapId] || namedMapVisuals[String(mapName || "").trim().toLowerCase()];
  if (!point || !visual?.size) return null;
  const lines = printedLabelLineLengths(name), longest = Math.max(...lines, 1), lineCount = lines.length;
  const baseWidth = point.width ?? Math.min(10, Math.max(2.6, longest * .44 + .8));
  const baseHeight = Math.max(point.height ?? 0, 1.2 + (lineCount - 1) * 1.75), [imageWidth, imageHeight] = visual.size;
  const toPixels = (part: PercentAnnotationPart | PercentAnnotationAnchor, width: number, height: number): PixelRect => ({x: (part.left - width / 2) / 100 * imageWidth, y: (part.top - height / 2) / 100 * imageHeight, width: width / 100 * imageWidth, height: height / 100 * imageHeight});
  const labelBounds = [toPixels(point, baseWidth, baseHeight), ...(point.parts || []).map(part => toPixels(part, part.width, part.height))];
  const hitBounds = labelBounds.map(rect => {const padX = Math.max(40, rect.width * .18), padY = Math.max(32, rect.height * .45); return {x: Math.max(0, rect.x - padX), y: Math.max(0, rect.y - padY), width: Math.min(imageWidth, rect.width + padX * 2), height: Math.min(imageHeight, rect.height + padY * 2)};});
  return {focusPoint: {x: (point.focusLeft ?? point.left) / 100 * imageWidth, y: (point.focusTop ?? point.top) / 100 * imageHeight}, labelBounds, hitBounds, lines: lineCount, revision: visual.revision};
}
function pixelRectStyle(rect: PixelRect, size: readonly [number, number]) {return {left: `${rect.x / size[0] * 100}%`, top: `${rect.y / size[1] * 100}%`, width: `${rect.width / size[0] * 100}%`, height: `${rect.height / size[1] * 100}%`};}
function OfflineZoomMap({selected, selectedExtract, keyFocus, onShowDetails}: {selected: any; selectedExtract: any; keyFocus?: KeyMapFocus | null; onShowDetails: () => void;}) {
  const viewportRef = useRef<HTMLDivElement | null>(null), markerRef = useRef<HTMLSpanElement | null>(null), dragRef = useRef<{x: number; y: number; startX: number; startY: number;} | null>(null), didDragRef = useRef(false), panCleanupRef = useRef<() => void>(() => {}), zoomAnchorRef = useRef<{x: number; y: number; localX: number; localY: number;} | null>(null), focusRequestRef = useRef("");
  const primaryUrl = mapImageFor({id: selected.id, name: selected.name, slug: selected.name.toLowerCase().replace(/\s+/g, "-")}), primaryVisual = mapVisualFor(selected), primaryVariant = useMemo<MapVariant>(() => ({id: "extraction", url: primaryUrl, title: "脱出地点・タスク対応マップ", kind: "基準版", width: primaryVisual?.size?.[0], height: primaryVisual?.size?.[1], source: "RE3MR / tarkov.dev", primary: true}), [primaryUrl, primaryVisual]);
  const [variants, setVariants] = useState<MapVariant[]>([primaryVariant]), [activeVariantId, setActiveVariantId] = useState("extraction"), [variantsLoading, setVariantsLoading] = useState(false), [variantsSourceUrl, setVariantsSourceUrl] = useState("");
  const activeVariant = variants.find(variant => variant.id === activeVariantId) || variants[0] || primaryVariant, remoteUrl = activeVariant.url, visual = activeVariant.primary ? primaryVisual : undefined, activeVariantIndex = Math.max(0, variants.findIndex(variant => variant.id === activeVariant.id)), sameKindOrdinal = activeVariant.primary ? 0 : variants.slice(0, activeVariantIndex + 1).filter(variant => variant.kind === activeVariant.kind).length, stableVariantTitle = activeVariant.primary ? "脱出地点・タスク対応マップ" : `${selected.ja} · ${activeVariant.kind}${sameKindOrdinal > 1 ? ` ${sameKindOrdinal}` : ""}`;
  const [zoom, setZoom] = useState(1), [imageUrl, setImageUrl] = useState(remoteUrl), [imageSize, setImageSize] = useState<readonly [number, number] | null>(null), [cached, setCached] = useState(false), [saving, setSaving] = useState(false), [message, setMessage] = useState(""), [imageHash, setImageHash] = useState(""), [imageError, setImageError] = useState(false);
  const clampZoom = (value: number) => Math.max(1, Math.min(3, value));
  const sizeMatches = !visual?.size || Boolean(imageSize && imageSize[0] === visual.size[0] && imageSize[1] === visual.size[1]);
  const selectedPoint = useMemo(() => activeVariant.primary && sizeMatches ? printedLabelAnchor(selected.id, selected.name, selectedExtract?.name) : null, [activeVariant, selected, selectedExtract, sizeMatches]);
  const keyPositions = useMemo(() => {const bounds = mapBounds[selected.id]; if (!activeVariant.primary || keyFocus?.mapId !== selected.id || !bounds) return []; return (keyFocus.positions || []).map((position, index) => {const [[startX, startZ], [endX, endZ]] = bounds; return {index, left: Math.max(1, Math.min(99, (position.x - startX) / (endX - startX) * 100)), top: Math.max(1, Math.min(99, (position.z - startZ) / (endZ - startZ) * 100)), raw: position};});}, [activeVariant, keyFocus, selected]);
  const focusTarget = useMemo(() => selectedPoint ? {left: selectedPoint.focusPoint.x / (visual?.size?.[0] || 1) * 100, top: selectedPoint.focusPoint.y / (visual?.size?.[1] || 1) * 100} : keyPositions[0] || null, [keyPositions, selectedPoint, visual]);
  const selectionKey = selectedExtract ? `${selected.id || selected.name}-${activeVariant.id}-${selectedExtract.id || selectedExtract.name}` : keyFocus && keyPositions.length ? `${selected.id}-${activeVariant.id}-${keyFocus.keyId}` : "", selectedLabelNotPrinted = Boolean(activeVariant.primary && unprintedLabelKeys[selected.id]?.has(printedLabelKey(selectedExtract?.name))), auditEnabled = useMemo(() => typeof window !== "undefined" && new URLSearchParams(window.location.search).has("mapAudit"), []);
  const auditAnnotations = useMemo(() => {if (!auditEnabled || !activeVariant.primary || !sizeMatches) return []; const annotationSource = printedLabelAnchors[selected.id] || printedLabelAnchors[`name:${printedLabelKey(selected.name)}`] || {}; return Object.keys(annotationSource).map(key => printedLabelAnchor(selected.id, selected.name, key)).filter(Boolean) as MapAnnotation[];}, [activeVariant, auditEnabled, selected, sizeMatches]);
  useEffect(() => {let cancelled = false; setVariants(current => current.length === 1 && current[0].id === primaryVariant.id && current[0].url === primaryVariant.url ? current : [primaryVariant]); setActiveVariantId(current => current === "extraction" ? current : "extraction"); setVariantsSourceUrl(current => current ? "" : current); if (!window.stashAI?.mapVariants) return; setVariantsLoading(true); const requestKey = `${selected.id || selected.name}:${selected.name}`; mapVariantRequests.get(requestKey, () => window.stashAI!.mapVariants(selected.name)).then(result => {if (cancelled) return; const primaryFileKey = printedLabelKey(decodeURIComponent(primaryUrl.split("/").pop()?.split("?")[0] || "")); const alternatives = (result.variants || []).filter((variant: MapVariant) => variant.url !== primaryUrl && printedLabelKey(variant.title) !== primaryFileKey); setVariants([primaryVariant, ...alternatives]); setVariantsSourceUrl(result.sourceUrl || "");}).catch(() => {}).finally(() => {if (!cancelled) setVariantsLoading(false);}); return () => {cancelled = true;};}, [primaryUrl, primaryVariant, selected.id, selected.name]);
  const cacheKey = `${selected.id || selected.name}-${activeVariant.id}:${remoteUrl}`;
  useEffect(() => {let cancelled = false; setZoom(1); setImageUrl(remoteUrl); setImageSize(null); setCached(false); setMessage(""); setImageHash(""); setImageError(false); if (viewportRef.current) {viewportRef.current.scrollLeft = 0; viewportRef.current.scrollTop = 0;} if (!window.stashAI?.cacheMapImage) return; mapImageCacheChecks.get(cacheKey, () => window.stashAI!.cacheMapImage(remoteUrl, `${selected.id || selected.name}-${activeVariant.id}`, false)).then(result => {if (cancelled || !result.cached) return; setImageUrl(result.url); setCached(true); setImageHash(result.sha256 || ""); setMessage("保存済み画像を表示中");}).catch(() => {}); return () => {cancelled = true;};}, [activeVariant.id, cacheKey, remoteUrl, selected.id, selected.name]);
  const saveOffline = async () => {if (!window.stashAI?.cacheMapImage) {setMessage("配布版アプリで利用できます"); return ;} setSaving(true); setMessage("保存中…"); try {const result = await window.stashAI.cacheMapImage(remoteUrl, `${selected.id || selected.name}-${activeVariant.id}`, true); if (!result.cached) throw Error(result.error || "保存失敗"); mapImageCacheChecks.delete(cacheKey); setImageUrl(result.url); setCached(true); setImageHash(result.sha256 || ""); setMessage(result.error ? "更新できなかったため保存済み画像を維持しています" : "オフライン保存完了");} catch {setMessage("保存できませんでした。現在の画像を維持します");} finally {setSaving(false);} };
  const zoomValueRef = useRef(zoom); zoomValueRef.current = zoom;
  const resetView = () => {zoomAnchorRef.current = null; focusRequestRef.current = ""; zoomValueRef.current = 1; if (zoom !== 1) setZoom(1); if (viewportRef.current) {viewportRef.current.scrollLeft = 0; viewportRef.current.scrollTop = 0;} };
  const zoomAt = (nextZoom: number, clientX?: number, clientY?: number) => {const viewport = viewportRef.current, next = clampZoom(nextZoom); if (!viewport || next === zoomValueRef.current) return; const rect = viewport.getBoundingClientRect(), localX = clientX === undefined ? viewport.clientWidth / 2 : clientX - rect.left, localY = clientY === undefined ? viewport.clientHeight / 2 : clientY - rect.top; zoomAnchorRef.current = {x: (viewport.scrollLeft + localX) / Math.max(1, viewport.scrollWidth), y: (viewport.scrollTop + localY) / Math.max(1, viewport.scrollHeight), localX, localY}; zoomValueRef.current = next; setZoom(next);};
  const zoomActionRef = useRef(zoomAt); zoomActionRef.current = zoomAt;
  useEffect(() => {const viewport = viewportRef.current; if (!viewport) return; const handleWheel = (event: WheelEvent) => {event.preventDefault(); event.stopPropagation(); if (!event.deltaY) return; zoomActionRef.current(zoomValueRef.current + (event.deltaY < 0 ? .2 : -.2), event.clientX, event.clientY);}; viewport.addEventListener("wheel", handleWheel, {passive: false}); return () => viewport.removeEventListener("wheel", handleWheel);}, []);
  useLayoutEffect(() => {const viewport = viewportRef.current, anchor = zoomAnchorRef.current; if (!viewport || !anchor) return; viewport.scrollLeft = anchor.x * viewport.scrollWidth - anchor.localX; viewport.scrollTop = anchor.y * viewport.scrollHeight - anchor.localY; zoomAnchorRef.current = null;}, [zoom]);
  useLayoutEffect(() => {if (!focusTarget || !selectionKey) return; focusRequestRef.current = selectionKey; zoomAnchorRef.current = null; if (zoomValueRef.current < 1.75) {zoomValueRef.current = 1.75; setZoom(1.75);} }, [focusTarget, selectionKey]);
  useLayoutEffect(() => {if (!focusTarget || !selectionKey || focusRequestRef.current !== selectionKey || zoom < 1.75) return; let secondFrame = 0; const firstFrame = requestAnimationFrame(() => {secondFrame = requestAnimationFrame(() => {const viewport = viewportRef.current, marker = markerRef.current; if (!viewport || !marker || focusRequestRef.current !== selectionKey) return; const left = marker.offsetLeft - viewport.clientWidth / 2, top = marker.offsetTop - viewport.clientHeight / 2; viewport.scrollTo({left: Math.max(0, Math.min(left, viewport.scrollWidth - viewport.clientWidth)), top: Math.max(0, Math.min(top, viewport.scrollHeight - viewport.clientHeight)), behavior: "smooth"}); focusRequestRef.current = "";});}); return () => {cancelAnimationFrame(firstFrame); cancelAnimationFrame(secondFrame);};}, [focusTarget, imageSize, selectionKey, zoom]);
  useEffect(() => () => panCleanupRef.current(), []);
  const startPan = (event: any) => {const viewport = viewportRef.current; if (event.button !== 0 || !viewport) return; event.preventDefault(); panCleanupRef.current(); didDragRef.current = false; dragRef.current = {x: event.clientX, y: event.clientY, startX: event.clientX, startY: event.clientY}; viewport.classList.add("panning"); let active = true; const move = (next: MouseEvent) => {const drag = dragRef.current, currentViewport = viewportRef.current; if (!active || !drag || !currentViewport) return; next.preventDefault(); if (Math.hypot(next.clientX - drag.startX, next.clientY - drag.startY) > 6) didDragRef.current = true; currentViewport.scrollLeft -= next.clientX - drag.x; currentViewport.scrollTop -= next.clientY - drag.y; drag.x = next.clientX; drag.y = next.clientY;}, stop = () => {if (!active) return; active = false; window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", stop); dragRef.current = null; viewport.classList.remove("panning"); if (panCleanupRef.current === stop) panCleanupRef.current = () => {};}; window.addEventListener("mousemove", move, {passive: false}); window.addEventListener("mouseup", stop, {once: true}); panCleanupRef.current = stop;};
  const markerPoints = useMemo(() => selectedPoint?.labelBounds || [], [selectedPoint]), hitTargets = useMemo(() => selectedPoint?.hitBounds || [], [selectedPoint]), visualSize = visual?.size;
  return <section className="zoomMapCard">
    <header><div><b>{stableVariantTitle}</b><small>{activeVariant.primary ? "脱出地点・タスク・鍵位置の表示に対応" : `${activeVariant.kind} · 閲覧用（位置表示は基準版のみ）`}</small></div><div className="mapZoomControls"><button type="button" onClick={() => zoomAt(zoom - .25)} disabled={zoom <= 1} aria-label="マップを縮小">−</button><span>{Math.round(zoom * 100)}%</span><button type="button" onClick={() => zoomAt(zoom + .25)} disabled={zoom >= 3} aria-label="マップを拡大">＋</button><button type="button" onClick={resetView}>全体表示</button></div></header>
    <div className="mapVariantPicker"><div><b>マップ画像を切り替え</b><small>{variantsLoading ? "英語Wikiから別バージョンを確認中…" : `${variants.length}種類`}</small></div><div>{variants.map((variant, index) => {const ordinal=variant.primary?0:variants.slice(0,index+1).filter(item=>item.kind===variant.kind).length,label=variant.primary?"脱出地点対応マップ":`${selected.ja} · ${variant.kind}${ordinal>1?` ${ordinal}`:""}`;return <button type="button" className={variant.id === activeVariant.id ? "active" : ""} key={variant.id} onClick={() => setActiveVariantId(variant.id)} title={variant.title}><i>{String(index + 1).padStart(2, "0")}</i><span><b>{variant.primary ? "脱出地点対応" : variant.kind}</b><small>{label}</small></span>{variant.primary && <em>機能付き</em>}</button>})}</div></div>
    <div ref={viewportRef} className={`zoomMapViewport ${panning ? "panning" : ""}`} onMouseDown={startPan}><div className="zoomMapInner" style={{width: `${zoom * 100}%`}}><img draggable={false} src={imageUrl} onLoad={event => {setImageSize([event.currentTarget.naturalWidth, event.currentTarget.naturalHeight]); setImageError(false);}} onError={() => {if (imageUrl !== remoteUrl) {setImageUrl(remoteUrl); setCached(false); setMessage("保存済み画像を読み込めなかったためオンライン画像を表示中"); setImageError(false);} else setImageError(true);}} alt={`${selected.name} ${stableVariantTitle}`} loading="eager" decoding="async" />
      {visualSize && !imageError && <><svg className="mapAnnotationOverlay" viewBox={`0 0 ${visualSize[0]} ${visualSize[1]}`} preserveAspectRatio="none" aria-hidden="true"><defs><filter id={`annotation-glow-${selected.id || printedLabelKey(selected.name)}`} x="-50%" y="-50%" width="200%" height="200%"><feGaussianBlur stdDeviation="8" result="blur"/><feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>{auditAnnotations.flatMap((annotation, annotationIndex) => annotation.labelBounds.map((rect, rectIndex) => <rect className="mapAuditRect" key={`audit-${annotationIndex}-${rectIndex}`} x={rect.x} y={rect.y} width={rect.width} height={rect.height} vectorEffect="non-scaling-stroke"/>))}{markerPoints.map((rect, index) => <rect className="printedLabelFrame" key={`${selectionKey}-frame-${index}`} x={rect.x} y={rect.y} width={rect.width} height={rect.height} rx="5" vectorEffect="non-scaling-stroke" filter={`url(#annotation-glow-${selected.id || printedLabelKey(selected.name)})`}/>)}</svg>{focusTarget && <span ref={markerRef} className="mapFocusAnchor" style={{left: `${focusTarget.left}%`, top: `${focusTarget.top}%`}} />}{hitTargets.map((rect, index) => <button key={`${selectionKey}-hit-${index}`} type="button" className="annotationHitTarget" style={pixelRectStyle(rect, visualSize)} onClick={event => {event.stopPropagation(); if (event.detail === 0 || !didDragRef.current) onShowDetails();}} aria-label={`${selectedExtract.name}の現地画像と詳細を見る${index ? ` ${index + 1}` : ""}`} title="クリックして現地画像と詳細を見る" />)}{keyPositions.map(point=><span className="keyLocationMarker" key={`${keyFocus?.keyId}-${point.index}`} style={{left:`${point.left}%`,top:`${point.top}%`}} title={`${keyFocus?.keyName} 使用位置 ${point.index+1}`}><i>KEY</i><b>{point.index+1}</b></span>)}</>}
    </div></div>
    {imageError && <div className="mapImageError"><b>マップ画像を表示できません</b><span>通信を確認するか、保存済み画像を利用してください。</span></div>}
    {selectedExtract && <div className="extractMapPreview" aria-live="polite"><div><small>選択中の脱出地点</small><strong>{selectedExtract.name}</strong><span>{!activeVariant.primary ? "脱出地点対応マップへ戻すと強調表示できます" : selectedPoint ? "文字を囲む枠をクリックすると詳細を確認できます" : !sizeMatches ? `画像版が異なります（対応版 ${visual?.revision || "未登録"}）` : selectedLabelNotPrinted ? "この画像には名称が印字されていないため、誤った枠は表示しません" : "この画像版の注釈を確認できません"}</span></div>{!activeVariant.primary && <button type="button" onClick={() => setActiveVariantId("extraction")}>脱出地点対応マップへ戻す</button>}</div>}
    {keyFocus && <div className="keyMapPreview" aria-live="polite"><div><small>鍵の使用場所</small><strong>{keyFocus.keyName}</strong><span>{!activeVariant.primary?"位置表示は基準版マップで確認できます":keyPositions.length?`${keyPositions.length}箇所をKEYマーカーで表示しています（API座標）`:"このマップの位置座標は登録されていません"}</span></div>{!activeVariant.primary&&<button type="button" onClick={()=>setActiveVariantId("extraction")}>基準版マップへ戻す</button>}</div>}
    <footer><div><span>ドラッグで移動</span><span>ホイール上で拡大</span><span>ホイール下で縮小</span>{visual && <span>画像版 {visual.revision}</span>}{activeVariant.width&&activeVariant.height&&<span>{activeVariant.width} × {activeVariant.height}</span>}{imageHash && <span title={imageHash}>SHA-256 {imageHash.slice(0, 10)}…</span>}{activeVariant.primary ? <a href="https://reemr.se/" target="_blank" rel="noreferrer">MAP: RE3MR ↗</a> : variantsSourceUrl && <a href={variantsSourceUrl} target="_blank" rel="noreferrer">英語WikiのMaps節 ↗</a>}</div><div className={`offlineSaveState ${cached ? "saved" : ""}`}><button type="button" disabled={saving} onClick={saveOffline}>{cached ? "↻ オフラインデータを更新" : "↓ このマップをオフライン保存"}</button>{message && <small>{message}</small>}</div></footer>
  </section>;
}

export default function EnhancedMapHub() {
  const restored = useMemo(() => {if (typeof window === "undefined") return {} as any; try {return JSON.parse(sessionStorage.getItem("tarkov-map-last-view") || "{}");} catch {return {} as any;} }, []), initialCollapsed = {PMC: true, Scav: true, All: true, Transit: true};
  const [selected, setSelected] = useState<any>(() => mapStageFor(history.state?.mapStage || restored.selected) || null), [keyFocus, setKeyFocus] = useState<KeyMapFocus | null>(() => history.state?.mapKeyFocus || null), [extract, setExtract] = useState<any>(null), [detailOpen, setDetailOpen] = useState(false), [extractGuide, setExtractGuide] = useState<any>(null), [guideLoading, setGuideLoading] = useState(false), [latest, setLatest] = useState<any[]>([]), [loading, setLoading] = useState(true), [mapDataStatus, setMapDataStatus] = useState<{source: string; updatedAt?: string | null; error?: string;}>({source: "読み込み中"}), [collapsedGroups, setCollapsedGroups] = useState<Record<string, boolean>>({...initialCollapsed, ...(restored.collapsedGroups || {})});
  useEffect(() => {let cancelled = false; const apply = (value: MapDataResult | any[], finishLoading = false) => {if (cancelled) return; const result = normalizeMapRuntime(value); if (result.maps.length) setLatest(result.maps); setMapDataStatus({source: result.source || "不明", updatedAt: result.updatedAt, error: result.error}); if (finishLoading) setLoading(false);}; const load = async () => {try {apply({maps: bundledMapData.maps, source: "同梱", updatedAt: bundledMapData.updatedAt}, true); if (!window.stashAI?.maps) return; apply(await window.stashAI.maps()); window.stashAI.refreshMaps?.().then(value => apply(value)).catch(() => {});} catch {setMapDataStatus(old => ({...old, error: "オンライン更新に失敗したため同梱データを表示しています"}));} finally {if (!cancelled) setLoading(false);}}; load(); return () => {cancelled = true;};}, []);
  useEffect(() => {sessionStorage.setItem("tarkov-map-last-view", JSON.stringify({selected: selected?.name || "", collapsedGroups}));}, [selected, collapsedGroups]);
  useEffect(() => {const restore = (event: PopStateEvent) => {if (event.state?.stashPage === "maps") {setSelected(mapStageFor(event.state?.mapStage) || null); setKeyFocus(event.state?.mapKeyFocus || null); setExtract(null);} }; window.addEventListener("popstate", restore); return () => window.removeEventListener("popstate", restore);}, []);
  useEffect(() => {let cancelled = false; setExtractGuide(null); if (detailOpen && extract && selected && window.stashAI?.extractGuide) {setGuideLoading(true); window.stashAI.extractGuide(selected.name, extract.name).then(guide => {if (!cancelled) setExtractGuide(guide);}).catch(() => {if (!cancelled) setExtractGuide(null);}).finally(() => {if (!cancelled) setGuideLoading(false);});} return () => {cancelled = true;};}, [detailOpen, extract?.name, selected?.name]);
  const openStage = (stage: any) => {history.pushState({...history.state, stashPage: "maps", mapStage: stage.name, mapKeyFocus: null}, ""); setSelected(stage); setKeyFocus(null); setExtract(null); setDetailOpen(false); setCollapsedGroups(initialCollapsed);};
  const mapKey = (value: any) => String(value || "").toLowerCase().replace(/^the\s+/, "").replace(/[^a-z0-9]/g, "");
  const source = selected && (latest.find(x => String(x.id || "") === String(selected.id || "__missing__")) || latest.find(x => sameMapLocation(x, selected))), liveExtracts = source?.extracts || [], selectedMapKey = mapKey(mapLocation(selected).name), manualExtracts = selectedMapKey === "factory" ? factoryExtracts : selectedMapKey === "lab" ? labExtracts : selectedMapKey === "labyrinth" ? labyrinthExtracts : null, extracts = manualExtracts ? manualExtracts.map(manual => ({...liveExtracts.find((live: any) => mapKey(live.name) === mapKey(manual.name)), ...manual})) : liveExtracts, transits = selectedMapKey === "factory" ? [] : (source?.transits || []), groups = [{key: "PMC", title: "PMC専用", className: "pmc", items: extracts.filter((x: any) => /^pmc$/i.test(x.faction))}, {key: "Scav", title: "SCAV専用", className: "scav", items: extracts.filter((x: any) => /scav/i.test(x.faction))}, {key: "All", title: "PMC・SCAV共通", className: "both", items: extracts.filter((x: any) => /all|both/i.test(x.faction))}, {key: "Transit", title: "他マップへのトランジット", className: "transit", items: transits}];
  if (selected) return <section className="mapHub mapDetail"><button className="mapBack" onClick={() => {setSelected(null); setKeyFocus(null); setExtract(null);}}>← 全マップへ戻る</button><header><div><small>ZOOMABLE MAP GUIDE</small><h2>{selected.ja}<span>{selected.name}</span></h2><p>{selected.desc}</p></div><div><b>{loading ? "脱出地点データを準備中" : mapDataStatus.error && !latest.length ? "脱出地点データを利用できません" : `${mapDataStatus.source}データを表示中`}</b>{mapDataStatus.updatedAt && <small>更新 {new Date(mapDataStatus.updatedAt).toLocaleString("ja-JP")}</small>}<small>{source?.players && `PMC ${source.players}`}</small><small>{source?.raidDuration && `制限時間 ${source.raidDuration}分`}</small></div></header><div className="mapInfoLayout"><div><OfflineZoomMap selected={selected} selectedExtract={extract} keyFocus={keyFocus} onShowDetails={() => setDetailOpen(true)} /><div className="mapBeginnerTips"><b>初心者向け確認ポイント</b><span>出撃地点を確認したら、最初に大きな建物・道路・地形から現在地を特定してください。</span><span>脱出地点はレイド中にOキーを2回押して、利用可能な名称を確認してください。</span></div></div><aside className="extractPanel grouped"><h3>脱出地点 <small>{extracts.length}か所{transits.length ? ` ＋ トランジット${transits.length}か所` : ""}</small></h3><div className="extractLegend"><span className="pmc">PMC専用</span><span className="scav">SCAV専用</span><span className="both">共通</span></div>{loading && selected.name !== "Factory" && <p>保存済みデータを読み込み中…</p>}{groups.map(group => group.items.length ? <section className={`extractGroup ${group.className} ${collapsedGroups[group.key] ? "collapsed" : ""}`} key={group.key}><button type="button" className="extractGroupToggle" aria-expanded={!collapsedGroups[group.key]} onClick={() => setCollapsedGroups(old => ({...old, [group.key]: !old[group.key]}))}><b>{group.title}</b><span>{group.items.length}か所</span><i>{collapsedGroups[group.key] ? "＋" : "−"}</i></button>{!collapsedGroups[group.key] && group.items.map((point: any, index: number) => <button key={`${point.name}-${index}`} className={extract === point ? "selected" : ""} onClick={() => {setKeyFocus(null); setExtract(point); setDetailOpen(false);}}><i></i><span><strong>{point.name}</strong><small>{point.direction || point.requirement || point.faction}</small></span><b>›</b></button>)}</section> : null)}{!loading && selected.name !== "Factory" && !liveExtracts.length && <p className="mapDataError">脱出地点データを取得できませんでした。アプリの同梱データも確認してください。</p>}</aside></div>{detailOpen && extract && <div className="extractModal detailDrawer" onClick={() => setDetailOpen(false)}><article onClick={event => event.stopPropagation()}><button onClick={() => setDetailOpen(false)} aria-label="詳細を閉じる">×</button><small>{extract.kind === "transit" ? "TRANSIT POINT" : "EXTRACTION POINT"} · {extract.faction}</small><h2>{extract.name}</h2><div className="extractGuideImages">{[...(extract.image ? [{url: extract.image, caption: `${extract.name} 現地画像`}] : []), ...(extractGuide?.images || [])].filter((image: any, index: number, list: any[]) => list.findIndex(x => x.url === image.url) === index).slice(0, 4).map((image: any, index: number) => <a href={image.url} target="_blank" rel="noreferrer" key={`${image.url}-${index}`}><i>{String(index + 1).padStart(2, "0")}</i><img src={image.url} alt={`${extract.name}の現地参考画像 ${index + 1}`} loading="lazy" decoding="async" /><span>{image.caption || `${extract.name} 参考画像 ${index + 1}`}<small>原寸表示</small></span></a>)}</div>{guideLoading && <p className="extractGuideLoading">現地画像をネットから検索しています…</p>}<div className="extractGuideLinks">{extractGuide?.sourceUrl && <a href={extractGuide.sourceUrl} target="_blank" rel="noreferrer">Wikiで確認 ↗</a>}{extractGuide?.searchUrl && <a href={extractGuide.searchUrl} target="_blank" rel="noreferrer">現地画像を検索 ↗</a>}</div> </article></div>}</section>;
  return <section className="mapHub"><header className="mapHubTitle"><small>MAP INTELLIGENCE</small><h2>マップを選択</h2><p>全体図のマップ名を囲む枠から選択してください。</p></header><div className="worldMap interactiveWorld"><img src={asset("maps/world-select.png")} alt="Tarkov地域の全体図" loading="eager" decoding="async" />{stageHotspots.map(hotspot => {const stage = mapStages.find(x => x.name === hotspot.name)!; return <button className="stageHotspot" aria-label={`${stage.ja}を開く`} title={`${stage.ja}の情報を見る`} key={hotspot.name} style={{left: `${hotspot.left}%`, top: `${hotspot.top}%`, width: `${hotspot.width}%`, height: `${hotspot.height}%`}} onClick={() => openStage(stage)}><span>{stage.name}</span></button>;})}</div><div className="additionalMaps"><span>全体図に記載のないマップ</span>{mapStages.filter(stage => !stageHotspots.some(x => x.name === stage.name)).map(stage => <button key={stage.name} onClick={() => openStage(stage)}>{stage.name}</button>)}</div></section>;
}

