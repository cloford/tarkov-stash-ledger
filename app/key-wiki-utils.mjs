export const normalizeKeyText=value=>String(value??"").normalize("NFKC").toLocaleLowerCase("ja-JP").replace(/[^\p{L}\p{N}]/gu,"");
export function filterKeys(keys,{query="",map="all",usage="all"}={}){const needle=normalizeKeyText(query);return (Array.isArray(keys)?keys:[]).filter(key=>{const haystack=normalizeKeyText([key.name,key.nameEn,key.shortName,key.description,key.descriptionEn,...(key.tasks||[]).flatMap(task=>[task.name,task.nameEn,task.trader]),...(key.mapUses||[]).flatMap(entry=>[entry.name,entry.nameEn])].join(" ")),matchesQuery=!needle||haystack.includes(needle),matchesMap=map==="all"||(key.mapUses||[]).some(entry=>entry.id===map),matchesUsage=usage==="all"||(usage==="task"?(key.tasks||[]).length>0:(key.tasks||[]).length===0);return matchesQuery&&matchesMap&&matchesUsage})}
